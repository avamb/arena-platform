// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: billing_ledger.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// TariffRow — result type for tariffs queries
// ─────────────────────────────────────────────────────────────────────────────

// TariffRow represents one versioned tariff plan.
//
// org_id = nil means the global default tariff.
// effective_from marks when this tariff version takes effect.
// The active tariff for a billing period is the one with the largest
// effective_from that is <= the period start date.
type TariffRow struct {
	ID                  uuid.UUID  `json:"id"`
	OrgID               *uuid.UUID `json:"org_id"`
	PlanName            string     `json:"plan_name"`
	EffectiveFrom       time.Time  `json:"effective_from"`
	PerTicketFeeMinor   int64      `json:"per_ticket_fee_minor"`
	PerEventFeeMinor    int64      `json:"per_event_fee_minor"`
	MonthlyFeeMinor     int64      `json:"monthly_fee_minor"`
	Currency            string     `json:"currency"`
	Notes               *string    `json:"notes"`
	CreatedAt           time.Time  `json:"created_at"`
	CreatedBy           *string    `json:"created_by"`
}

// scanTariffRow scans a single tariffs row into a TariffRow.
func scanTariffRow(row interface {
	Scan(dest ...any) error
}) (TariffRow, error) {
	var r TariffRow
	err := row.Scan(
		&r.ID,
		&r.OrgID,
		&r.PlanName,
		&r.EffectiveFrom,
		&r.PerTicketFeeMinor,
		&r.PerEventFeeMinor,
		&r.MonthlyFeeMinor,
		&r.Currency,
		&r.Notes,
		&r.CreatedAt,
		&r.CreatedBy,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// UsageRecordRow — result type for usage_records queries
// ─────────────────────────────────────────────────────────────────────────────

// UsageRecordRow represents monthly usage counters for one org and billing period.
// billing_period is 'YYYY-MM' (e.g. '2026-06').
type UsageRecordRow struct {
	ID                  uuid.UUID `json:"id"`
	OrgID               uuid.UUID `json:"org_id"`
	BillingPeriod       string    `json:"billing_period"`
	TicketsSold         int64     `json:"tickets_sold"`
	ComplimentaryIssued int64     `json:"complimentary_issued"`
	EventsPublished     int64     `json:"events_published"`
	CreatedAt           time.Time `json:"created_at"`
	UpdatedAt           time.Time `json:"updated_at"`
}

// scanUsageRecordRow scans a single usage_records row into a UsageRecordRow.
func scanUsageRecordRow(row interface {
	Scan(dest ...any) error
}) (UsageRecordRow, error) {
	var r UsageRecordRow
	err := row.Scan(
		&r.ID,
		&r.OrgID,
		&r.BillingPeriod,
		&r.TicketsSold,
		&r.ComplimentaryIssued,
		&r.EventsPublished,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InvoiceRow — result type for invoices queries
// ─────────────────────────────────────────────────────────────────────────────

// InvoiceRow represents a billing invoice header.
//
// State machine: draft → issued → paid (terminal). draft/issued → void (terminal).
// TotalAmountMinor is in the minor unit of Currency (e.g. cents for EUR/USD).
type InvoiceRow struct {
	ID                uuid.UUID  `json:"id"`
	OrgID             uuid.UUID  `json:"org_id"`
	BillingPeriod     string     `json:"billing_period"`
	State             string     `json:"state"`
	TotalAmountMinor  int64      `json:"total_amount_minor"`
	Currency          string     `json:"currency"`
	IssuedAt          *time.Time `json:"issued_at"`
	PaidAt            *time.Time `json:"paid_at"`
	VoidedAt          *time.Time `json:"voided_at"`
	CreatedAt         time.Time  `json:"created_at"`
	UpdatedAt         time.Time  `json:"updated_at"`
}

// scanInvoiceRow scans a single invoices row into an InvoiceRow.
func scanInvoiceRow(row interface {
	Scan(dest ...any) error
}) (InvoiceRow, error) {
	var r InvoiceRow
	err := row.Scan(
		&r.ID,
		&r.OrgID,
		&r.BillingPeriod,
		&r.State,
		&r.TotalAmountMinor,
		&r.Currency,
		&r.IssuedAt,
		&r.PaidAt,
		&r.VoidedAt,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InvoiceLineRow — result type for invoice_lines queries
// ─────────────────────────────────────────────────────────────────────────────

// InvoiceLineRow represents a single line item on an invoice.
//
// tariff_id links to the tariff version used for this calculation, enabling
// reproducible auditing even after the tariff is updated.
type InvoiceLineRow struct {
	ID               uuid.UUID  `json:"id"`
	InvoiceID        uuid.UUID  `json:"invoice_id"`
	TariffID         *uuid.UUID `json:"tariff_id"`
	Description      string     `json:"description"`
	Quantity         int64      `json:"quantity"`
	UnitAmountMinor  int64      `json:"unit_amount_minor"`
	TotalAmountMinor int64      `json:"total_amount_minor"`
	Currency         string     `json:"currency"`
	CreatedAt        time.Time  `json:"created_at"`
}

// scanInvoiceLineRow scans a single invoice_lines row into an InvoiceLineRow.
func scanInvoiceLineRow(row interface {
	Scan(dest ...any) error
}) (InvoiceLineRow, error) {
	var r InvoiceLineRow
	err := row.Scan(
		&r.ID,
		&r.InvoiceID,
		&r.TariffID,
		&r.Description,
		&r.Quantity,
		&r.UnitAmountMinor,
		&r.TotalAmountMinor,
		&r.Currency,
		&r.CreatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertTariff
// ─────────────────────────────────────────────────────────────────────────────

const insertTariff = `-- name: InsertTariff :one
INSERT INTO tariffs (
    org_id, plan_name, effective_from,
    per_ticket_fee_minor, per_event_fee_minor, monthly_fee_minor,
    currency, notes, created_by
)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8, $9)
RETURNING id, org_id, plan_name, effective_from,
          per_ticket_fee_minor, per_event_fee_minor, monthly_fee_minor,
          currency, notes, created_at, created_by`

// InsertTariff creates a new tariff version.
// Pass orgID = nil for the global default tariff (applies to all orgs without
// a specific override). Pass notes and createdBy as nil when not provided.
func (q *Queries) InsertTariff(
	ctx context.Context,
	orgID *uuid.UUID,
	planName string,
	effectiveFrom time.Time,
	perTicketFeeMinor int64,
	perEventFeeMinor int64,
	monthlyFeeMinor int64,
	currency string,
	notes *string,
	createdBy *string,
) (TariffRow, error) {
	row := q.db.QueryRow(ctx, insertTariff,
		orgID, planName, effectiveFrom,
		perTicketFeeMinor, perEventFeeMinor, monthlyFeeMinor,
		currency, notes, createdBy,
	)
	return scanTariffRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetActiveTariff
// ─────────────────────────────────────────────────────────────────────────────

const getActiveTariff = `-- name: GetActiveTariff :one
SELECT id, org_id, plan_name, effective_from,
       per_ticket_fee_minor, per_event_fee_minor, monthly_fee_minor,
       currency, notes, created_at, created_by
FROM   tariffs
WHERE  (org_id = $1 OR org_id IS NULL)
  AND  effective_from <= $2
ORDER BY (org_id IS NULL) ASC, effective_from DESC
LIMIT  1`

// GetActiveTariff returns the most recent tariff for orgID effective on or before
// asOfDate. Falls back to the global default (org_id IS NULL) when no org-specific
// tariff exists. Returns pgx.ErrNoRows when no tariff has been configured.
func (q *Queries) GetActiveTariff(ctx context.Context, orgID uuid.UUID, asOfDate time.Time) (TariffRow, error) {
	row := q.db.QueryRow(ctx, getActiveTariff, orgID, asOfDate)
	return scanTariffRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetTariffByID
// ─────────────────────────────────────────────────────────────────────────────

const getTariffByID = `-- name: GetTariffByID :one
SELECT id, org_id, plan_name, effective_from,
       per_ticket_fee_minor, per_event_fee_minor, monthly_fee_minor,
       currency, notes, created_at, created_by
FROM   tariffs
WHERE  id = $1`

// GetTariffByID fetches a tariff by its UUID primary key.
// Returns pgx.ErrNoRows when not found.
func (q *Queries) GetTariffByID(ctx context.Context, id uuid.UUID) (TariffRow, error) {
	row := q.db.QueryRow(ctx, getTariffByID, id)
	return scanTariffRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListTariffs
// ─────────────────────────────────────────────────────────────────────────────

const listTariffs = `-- name: ListTariffs :many
SELECT id, org_id, plan_name, effective_from,
       per_ticket_fee_minor, per_event_fee_minor, monthly_fee_minor,
       currency, notes, created_at, created_by
FROM   tariffs
WHERE  org_id IS NOT DISTINCT FROM $1
ORDER BY effective_from DESC`

// ListTariffs returns all tariff versions for a given org_id scope.
// Pass orgID = nil to list global default tariffs. Returns newest-first.
func (q *Queries) ListTariffs(ctx context.Context, orgID *uuid.UUID) ([]TariffRow, error) {
	rows, err := q.db.Query(ctx, listTariffs, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []TariffRow
	for rows.Next() {
		r, err := scanTariffRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// IncrementUsageRecord
// ─────────────────────────────────────────────────────────────────────────────

const incrementUsageRecord = `-- name: IncrementUsageRecord :one
INSERT INTO usage_records (org_id, billing_period, tickets_sold, complimentary_issued, events_published)
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (org_id, billing_period) DO UPDATE
    SET tickets_sold         = usage_records.tickets_sold         + EXCLUDED.tickets_sold,
        complimentary_issued = usage_records.complimentary_issued + EXCLUDED.complimentary_issued,
        events_published     = usage_records.events_published     + EXCLUDED.events_published,
        updated_at           = now()
RETURNING id, org_id, billing_period, tickets_sold, complimentary_issued, events_published, created_at, updated_at`

// IncrementUsageRecord atomically increments usage counters for (orgID, billingPeriod).
// Creates the row if it doesn't exist yet (UPSERT). Pass 0 for counters that
// don't need incrementing (e.g. deltaComplimentary=0 when only ticketsSold changed).
// Returns the updated row with the new cumulative totals.
func (q *Queries) IncrementUsageRecord(
	ctx context.Context,
	orgID uuid.UUID,
	billingPeriod string,
	deltaTickets int64,
	deltaComplimentary int64,
	deltaEvents int64,
) (UsageRecordRow, error) {
	row := q.db.QueryRow(ctx, incrementUsageRecord,
		orgID, billingPeriod, deltaTickets, deltaComplimentary, deltaEvents,
	)
	return scanUsageRecordRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetUsageRecord
// ─────────────────────────────────────────────────────────────────────────────

const getUsageRecord = `-- name: GetUsageRecord :one
SELECT id, org_id, billing_period, tickets_sold, complimentary_issued, events_published, created_at, updated_at
FROM   usage_records
WHERE  org_id        = $1
  AND  billing_period = $2`

// GetUsageRecord fetches usage for one org and billing period.
// Returns pgx.ErrNoRows when no usage has been recorded yet.
func (q *Queries) GetUsageRecord(ctx context.Context, orgID uuid.UUID, billingPeriod string) (UsageRecordRow, error) {
	row := q.db.QueryRow(ctx, getUsageRecord, orgID, billingPeriod)
	return scanUsageRecordRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListUsageRecordsByPeriod
// ─────────────────────────────────────────────────────────────────────────────

const listUsageRecordsByPeriod = `-- name: ListUsageRecordsByPeriod :many
SELECT id, org_id, billing_period, tickets_sold, complimentary_issued, events_published, created_at, updated_at
FROM   usage_records
WHERE  billing_period = $1
ORDER BY org_id`

// ListUsageRecordsByPeriod returns all orgs that have usage in the given billing
// period. Used by the month-end batch worker to determine which orgs need an invoice.
func (q *Queries) ListUsageRecordsByPeriod(ctx context.Context, billingPeriod string) ([]UsageRecordRow, error) {
	rows, err := q.db.Query(ctx, listUsageRecordsByPeriod, billingPeriod)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []UsageRecordRow
	for rows.Next() {
		r, err := scanUsageRecordRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertInvoice
// ─────────────────────────────────────────────────────────────────────────────

const insertInvoice = `-- name: InsertInvoice :one
INSERT INTO invoices (org_id, billing_period, total_amount_minor, currency)
VALUES ($1, $2, $3, $4)
RETURNING id, org_id, billing_period, state, total_amount_minor, currency,
          issued_at, paid_at, voided_at, created_at, updated_at`

// InsertInvoice creates a new invoice in 'draft' state.
// totalAmountMinor may be 0 initially; use UpdateInvoiceTotal to finalize
// after inserting all lines.
func (q *Queries) InsertInvoice(
	ctx context.Context,
	orgID uuid.UUID,
	billingPeriod string,
	totalAmountMinor int64,
	currency string,
) (InvoiceRow, error) {
	row := q.db.QueryRow(ctx, insertInvoice,
		orgID, billingPeriod, totalAmountMinor, currency,
	)
	return scanInvoiceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetInvoiceByID
// ─────────────────────────────────────────────────────────────────────────────

const getInvoiceByID = `-- name: GetInvoiceByID :one
SELECT id, org_id, billing_period, state, total_amount_minor, currency,
       issued_at, paid_at, voided_at, created_at, updated_at
FROM   invoices
WHERE  id = $1`

// GetInvoiceByID fetches an invoice by its UUID primary key.
// Returns pgx.ErrNoRows when not found.
func (q *Queries) GetInvoiceByID(ctx context.Context, id uuid.UUID) (InvoiceRow, error) {
	row := q.db.QueryRow(ctx, getInvoiceByID, id)
	return scanInvoiceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetInvoiceByOrgAndPeriod
// ─────────────────────────────────────────────────────────────────────────────

const getInvoiceByOrgAndPeriod = `-- name: GetInvoiceByOrgAndPeriod :one
SELECT id, org_id, billing_period, state, total_amount_minor, currency,
       issued_at, paid_at, voided_at, created_at, updated_at
FROM   invoices
WHERE  org_id = $1 AND billing_period = $2`

// GetInvoiceByOrgAndPeriod fetches the invoice for one org and billing period.
// Returns pgx.ErrNoRows when not found. Used to prevent duplicate invoice generation.
func (q *Queries) GetInvoiceByOrgAndPeriod(ctx context.Context, orgID uuid.UUID, billingPeriod string) (InvoiceRow, error) {
	row := q.db.QueryRow(ctx, getInvoiceByOrgAndPeriod, orgID, billingPeriod)
	return scanInvoiceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListInvoicesByOrg
// ─────────────────────────────────────────────────────────────────────────────

const listInvoicesByOrg = `-- name: ListInvoicesByOrg :many
SELECT id, org_id, billing_period, state, total_amount_minor, currency,
       issued_at, paid_at, voided_at, created_at, updated_at
FROM   invoices
WHERE  org_id = $1
ORDER BY billing_period DESC, created_at DESC`

// ListInvoicesByOrg returns all invoices for the given org, newest period first.
func (q *Queries) ListInvoicesByOrg(ctx context.Context, orgID uuid.UUID) ([]InvoiceRow, error) {
	rows, err := q.db.Query(ctx, listInvoicesByOrg, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []InvoiceRow
	for rows.Next() {
		r, err := scanInvoiceRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdateInvoiceState
// ─────────────────────────────────────────────────────────────────────────────

const updateInvoiceState = `-- name: UpdateInvoiceState :one
UPDATE invoices
SET    state      = $2,
       updated_at = now(),
       issued_at  = CASE WHEN $2 = 'issued' THEN now() ELSE issued_at END,
       paid_at    = CASE WHEN $2 = 'paid'   THEN now() ELSE paid_at   END,
       voided_at  = CASE WHEN $2 = 'void'   THEN now() ELSE voided_at END
WHERE  id = $1
RETURNING id, org_id, billing_period, state, total_amount_minor, currency,
          issued_at, paid_at, voided_at, created_at, updated_at`

// UpdateInvoiceState advances an invoice to a new state.
// Timestamps (issued_at, paid_at, voided_at) are set automatically.
// Returns pgx.ErrNoRows when the invoice does not exist.
func (q *Queries) UpdateInvoiceState(ctx context.Context, id uuid.UUID, newState string) (InvoiceRow, error) {
	row := q.db.QueryRow(ctx, updateInvoiceState, id, newState)
	return scanInvoiceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdateInvoiceTotal
// ─────────────────────────────────────────────────────────────────────────────

const updateInvoiceTotal = `-- name: UpdateInvoiceTotal :one
UPDATE invoices
SET    total_amount_minor = $2,
       updated_at         = now()
WHERE  id = $1
RETURNING id, org_id, billing_period, state, total_amount_minor, currency,
          issued_at, paid_at, voided_at, created_at, updated_at`

// UpdateInvoiceTotal sets the total amount on an invoice.
// Called after all invoice lines have been inserted to finalize the total.
// Returns pgx.ErrNoRows when the invoice does not exist.
func (q *Queries) UpdateInvoiceTotal(ctx context.Context, id uuid.UUID, totalAmountMinor int64) (InvoiceRow, error) {
	row := q.db.QueryRow(ctx, updateInvoiceTotal, id, totalAmountMinor)
	return scanInvoiceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertInvoiceLine
// ─────────────────────────────────────────────────────────────────────────────

const insertInvoiceLine = `-- name: InsertInvoiceLine :one
INSERT INTO invoice_lines (
    invoice_id, tariff_id, description, quantity,
    unit_amount_minor, total_amount_minor, currency
)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, invoice_id, tariff_id, description, quantity,
          unit_amount_minor, total_amount_minor, currency, created_at`

// InsertInvoiceLine appends a line item to an invoice.
// Pass tariffID = nil for manual/adjustment lines not linked to a tariff version.
// totalAmountMinor should equal quantity * unitAmountMinor (caller responsibility).
func (q *Queries) InsertInvoiceLine(
	ctx context.Context,
	invoiceID uuid.UUID,
	tariffID *uuid.UUID,
	description string,
	quantity int64,
	unitAmountMinor int64,
	totalAmountMinor int64,
	currency string,
) (InvoiceLineRow, error) {
	row := q.db.QueryRow(ctx, insertInvoiceLine,
		invoiceID, tariffID, description, quantity,
		unitAmountMinor, totalAmountMinor, currency,
	)
	return scanInvoiceLineRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListInvoiceLines
// ─────────────────────────────────────────────────────────────────────────────

const listInvoiceLines = `-- name: ListInvoiceLines :many
SELECT id, invoice_id, tariff_id, description, quantity,
       unit_amount_minor, total_amount_minor, currency, created_at
FROM   invoice_lines
WHERE  invoice_id = $1
ORDER BY created_at ASC, id ASC`

// ListInvoiceLines returns all line items for the given invoice in insertion order.
func (q *Queries) ListInvoiceLines(ctx context.Context, invoiceID uuid.UUID) ([]InvoiceLineRow, error) {
	rows, err := q.db.Query(ctx, listInvoiceLines, invoiceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []InvoiceLineRow
	for rows.Next() {
		r, err := scanInvoiceLineRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}
