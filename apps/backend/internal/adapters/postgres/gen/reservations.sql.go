// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: reservations.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// ReservationRow — shared result type for all reservations queries
// ─────────────────────────────────────────────────────────────────────────────

// ReservationRow is the result type returned by all reservations queries.
//
// tier_id is nil for session-level GA reservations and non-nil for tier-scoped ones.
// user_id is nil when created anonymously (e.g. from a public feed token without login).
//
// State machine: draft → active → converted|expired|cancelled.
// cancelled_at, converted_at, expired_at are nil until the corresponding transition occurs.
type ReservationRow struct {
	ID          uuid.UUID  `json:"id"`
	OrgID       uuid.UUID  `json:"org_id"`
	ChannelID   uuid.UUID  `json:"channel_id"`
	SessionID   uuid.UUID  `json:"session_id"`
	TierID      *uuid.UUID `json:"tier_id"`
	UserID      *uuid.UUID `json:"user_id"`
	Quantity    int32      `json:"quantity"`
	State       string     `json:"state"`
	ExpiresAt   time.Time  `json:"expires_at"`
	CreatedAt   time.Time  `json:"created_at"`
	UpdatedAt   time.Time  `json:"updated_at"`
	CancelledAt *time.Time `json:"cancelled_at"`
	ConvertedAt *time.Time `json:"converted_at"`
	ExpiredAt   *time.Time `json:"expired_at"`
}

// scanReservationRow scans a single reservations row into a ReservationRow.
func scanReservationRow(row interface {
	Scan(dest ...any) error
}) (ReservationRow, error) {
	var r ReservationRow
	err := row.Scan(
		&r.ID,
		&r.OrgID,
		&r.ChannelID,
		&r.SessionID,
		&r.TierID,
		&r.UserID,
		&r.Quantity,
		&r.State,
		&r.ExpiresAt,
		&r.CreatedAt,
		&r.UpdatedAt,
		&r.CancelledAt,
		&r.ConvertedAt,
		&r.ExpiredAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertReservation
// ─────────────────────────────────────────────────────────────────────────────

const insertReservation = `-- name: InsertReservation :one
INSERT INTO reservations (org_id, channel_id, session_id, tier_id, user_id, quantity, expires_at)
VALUES ($1, $2, $3, $4, $5, $6, $7)
RETURNING id, org_id, channel_id, session_id, tier_id, user_id, quantity, state,
          expires_at, created_at, updated_at, cancelled_at, converted_at, expired_at`

// InsertReservation creates a new reservation in 'draft' state.
// Pass tierID = nil for a session-level GA reservation (no tier).
// Pass userID = nil for anonymous reservations.
// expires_at is computed by the caller from org/channel TTL settings or the
// system default of 1200s (20 min).
// Returns the created row including the uuidv7 PK assigned by the database.
func (q *Queries) InsertReservation(
	ctx context.Context,
	orgID, channelID, sessionID uuid.UUID,
	tierID, userID *uuid.UUID,
	quantity int32,
	expiresAt time.Time,
) (ReservationRow, error) {
	row := q.db.QueryRow(ctx, insertReservation,
		orgID, channelID, sessionID, tierID, userID, quantity, expiresAt,
	)
	return scanReservationRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetReservationByID
// ─────────────────────────────────────────────────────────────────────────────

const getReservationByID = `-- name: GetReservationByID :one
SELECT id, org_id, channel_id, session_id, tier_id, user_id, quantity, state,
       expires_at, created_at, updated_at, cancelled_at, converted_at, expired_at
FROM   reservations
WHERE  id = $1`

// GetReservationByID fetches a single reservation by its UUID primary key.
// Returns pgx.ErrNoRows when not found.
func (q *Queries) GetReservationByID(ctx context.Context, id uuid.UUID) (ReservationRow, error) {
	row := q.db.QueryRow(ctx, getReservationByID, id)
	return scanReservationRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdateReservationState
// ─────────────────────────────────────────────────────────────────────────────

const updateReservationState = `-- name: UpdateReservationState :one
UPDATE reservations
SET    state        = $2,
       updated_at   = now(),
       cancelled_at = CASE WHEN $2 = 'cancelled' THEN now() ELSE cancelled_at END,
       converted_at = CASE WHEN $2 = 'converted' THEN now() ELSE converted_at END,
       expired_at   = CASE WHEN $2 = 'expired'   THEN now() ELSE expired_at   END
WHERE  id = $1
RETURNING id, org_id, channel_id, session_id, tier_id, user_id, quantity, state,
          expires_at, created_at, updated_at, cancelled_at, converted_at, expired_at`

// UpdateReservationState transitions the reservation to a new state.
// The appropriate timestamp column (cancelled_at, converted_at, or expired_at) is
// automatically set via CASE in the SQL.
// Returns pgx.ErrNoRows when the reservation does not exist.
func (q *Queries) UpdateReservationState(ctx context.Context, id uuid.UUID, state string) (ReservationRow, error) {
	row := q.db.QueryRow(ctx, updateReservationState, id, state)
	return scanReservationRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetExpiredReservations
// ─────────────────────────────────────────────────────────────────────────────

const getExpiredReservations = `-- name: GetExpiredReservations :many
SELECT id, org_id, channel_id, session_id, tier_id, user_id, quantity, state,
       expires_at, created_at, updated_at, cancelled_at, converted_at, expired_at
FROM   reservations
WHERE  state IN ('draft', 'active')
  AND  expires_at < now()
ORDER BY expires_at ASC
LIMIT  $1
FOR UPDATE SKIP LOCKED`

// GetExpiredReservations polls up to limit reservations whose TTL has elapsed.
// Uses FOR UPDATE SKIP LOCKED so concurrent TTL worker instances never
// double-process the same reservation.
// Must be called inside a transaction.
func (q *Queries) GetExpiredReservations(ctx context.Context, limit int32) ([]ReservationRow, error) {
	rows, err := q.db.Query(ctx, getExpiredReservations, limit)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var reservations []ReservationRow
	for rows.Next() {
		r, err := scanReservationRow(rows)
		if err != nil {
			return nil, err
		}
		reservations = append(reservations, r)
	}
	return reservations, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// ListReservationsBySession
// ─────────────────────────────────────────────────────────────────────────────

const listReservationsBySession = `-- name: ListReservationsBySession :many
SELECT id, org_id, channel_id, session_id, tier_id, user_id, quantity, state,
       expires_at, created_at, updated_at, cancelled_at, converted_at, expired_at
FROM   reservations
WHERE  session_id = $1
ORDER BY created_at DESC, id DESC`

// ListReservationsBySession returns all reservations for the given session,
// newest first.
func (q *Queries) ListReservationsBySession(ctx context.Context, sessionID uuid.UUID) ([]ReservationRow, error) {
	rows, err := q.db.Query(ctx, listReservationsBySession, sessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var reservations []ReservationRow
	for rows.Next() {
		r, err := scanReservationRow(rows)
		if err != nil {
			return nil, err
		}
		reservations = append(reservations, r)
	}
	return reservations, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// ListReservationsByUser
// ─────────────────────────────────────────────────────────────────────────────

const listReservationsByUser = `-- name: ListReservationsByUser :many
SELECT id, org_id, channel_id, session_id, tier_id, user_id, quantity, state,
       expires_at, created_at, updated_at, cancelled_at, converted_at, expired_at
FROM   reservations
WHERE  user_id = $1
ORDER BY created_at DESC, id DESC`

// ListReservationsByUser returns all reservations for the given user,
// newest first.
func (q *Queries) ListReservationsByUser(ctx context.Context, userID uuid.UUID) ([]ReservationRow, error) {
	rows, err := q.db.Query(ctx, listReservationsByUser, userID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var reservations []ReservationRow
	for rows.Next() {
		r, err := scanReservationRow(rows)
		if err != nil {
			return nil, err
		}
		reservations = append(reservations, r)
	}
	return reservations, rows.Err()
}
