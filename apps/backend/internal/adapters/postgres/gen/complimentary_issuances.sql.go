// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: complimentary_issuances.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// ComplimentaryIssuanceRow — shared result type for complimentary_issuances queries
// ─────────────────────────────────────────────────────────────────────────────

// ComplimentaryIssuanceRow is the result type returned by complimentary_issuances queries.
//
// Status lifecycle: pending → issued | failed.
// Idempotency is enforced by the (org_id, batch_id) unique index.
// Inventory is decremented atomically via ReserveCapacity + ConfirmCapacity.
type ComplimentaryIssuanceRow struct {
	ID         uuid.UUID  `json:"id"`
	OrgID      uuid.UUID  `json:"org_id"`
	SessionID  uuid.UUID  `json:"session_id"`
	TierID     *uuid.UUID `json:"tier_id"`
	Qty        int32      `json:"qty"`
	Recipients []string   `json:"recipients"`
	BatchID    string     `json:"batch_id"`
	Status     string     `json:"status"`
	IssuedBy   *string    `json:"issued_by"`
	Notes      *string    `json:"notes"`
	CreatedAt  time.Time  `json:"created_at"`
	UpdatedAt  time.Time  `json:"updated_at"`
}

// scanComplimentaryIssuanceRow scans a single complimentary_issuances row.
func scanComplimentaryIssuanceRow(row interface {
	Scan(dest ...any) error
}) (ComplimentaryIssuanceRow, error) {
	var r ComplimentaryIssuanceRow
	err := row.Scan(
		&r.ID,
		&r.OrgID,
		&r.SessionID,
		&r.TierID,
		&r.Qty,
		&r.Recipients,
		&r.BatchID,
		&r.Status,
		&r.IssuedBy,
		&r.Notes,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// ComplimentaryTicketRow — ticket row returned by InsertComplimentaryTicket
// ─────────────────────────────────────────────────────────────────────────────

// ComplimentaryTicketRow is the result type for InsertComplimentaryTicket and
// ListTicketsByComplimentaryIssuance. It mirrors TicketRow but with
// ComplimentaryIssuanceID instead of CheckoutSessionID, as complimentary
// tickets are sourced from a complimentary_issuances batch (no checkout session).
type ComplimentaryTicketRow struct {
	ID                      uuid.UUID  `json:"id"`
	ComplimentaryIssuanceID uuid.UUID  `json:"complimentary_issuance_id"`
	SessionID               uuid.UUID  `json:"session_id"`
	TierID                  *uuid.UUID `json:"tier_id"`
	HolderEmail             *string    `json:"holder_email"`
	Status                  string     `json:"status"`
	IssuedAt                time.Time  `json:"issued_at"`
	CreatedAt               time.Time  `json:"created_at"`
	UpdatedAt               time.Time  `json:"updated_at"`
}

// scanComplimentaryTicketRow scans a single tickets row for complimentary issuances.
func scanComplimentaryTicketRow(row interface {
	Scan(dest ...any) error
}) (ComplimentaryTicketRow, error) {
	var r ComplimentaryTicketRow
	err := row.Scan(
		&r.ID,
		&r.ComplimentaryIssuanceID,
		&r.SessionID,
		&r.TierID,
		&r.HolderEmail,
		&r.Status,
		&r.IssuedAt,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertComplimentaryIssuance
// ─────────────────────────────────────────────────────────────────────────────

// InsertComplimentaryIssuance creates a new complimentary issuance batch in 'pending' status.
//
// Pass tierID = nil for a session-level (untiered) issuance.
// Pass issuedBy = nil when the actor identity is not available.
// Pass notes = nil when no annotation is needed.
// Returns the created row including the uuidv7 PK assigned by the database.
func (q *Queries) InsertComplimentaryIssuance(
	ctx context.Context,
	orgID uuid.UUID,
	sessionID uuid.UUID,
	tierID *uuid.UUID,
	qty int32,
	recipients []string,
	batchID string,
	issuedBy *string,
	notes *string,
) (ComplimentaryIssuanceRow, error) {
	const sql = `
INSERT INTO complimentary_issuances (org_id, session_id, tier_id, qty, recipients, batch_id, issued_by, notes)
VALUES ($1, $2, $3, $4, $5, $6, $7, $8)
RETURNING id, org_id, session_id, tier_id, qty, recipients, batch_id, status, issued_by, notes,
          created_at, updated_at`
	row := q.db.QueryRow(ctx, sql,
		orgID, sessionID, tierID, qty, recipients, batchID, issuedBy, notes,
	)
	return scanComplimentaryIssuanceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetComplimentaryIssuanceByBatchID
// ─────────────────────────────────────────────────────────────────────────────

// GetComplimentaryIssuanceByBatchID fetches a complimentary issuance by (org_id, batch_id).
// This is the idempotency check: if a row exists, return it without re-issuing.
// Returns pgx.ErrNoRows when no matching row exists.
func (q *Queries) GetComplimentaryIssuanceByBatchID(
	ctx context.Context,
	orgID uuid.UUID,
	batchID string,
) (ComplimentaryIssuanceRow, error) {
	const sql = `
SELECT id, org_id, session_id, tier_id, qty, recipients, batch_id, status, issued_by, notes,
       created_at, updated_at
FROM   complimentary_issuances
WHERE  org_id = $1
  AND  batch_id = $2`
	row := q.db.QueryRow(ctx, sql, orgID, batchID)
	return scanComplimentaryIssuanceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetComplimentaryIssuanceByID
// ─────────────────────────────────────────────────────────────────────────────

// GetComplimentaryIssuanceByID fetches a single complimentary issuance by UUID.
// Returns pgx.ErrNoRows when not found.
func (q *Queries) GetComplimentaryIssuanceByID(
	ctx context.Context,
	id uuid.UUID,
) (ComplimentaryIssuanceRow, error) {
	const sql = `
SELECT id, org_id, session_id, tier_id, qty, recipients, batch_id, status, issued_by, notes,
       created_at, updated_at
FROM   complimentary_issuances
WHERE  id = $1`
	row := q.db.QueryRow(ctx, sql, id)
	return scanComplimentaryIssuanceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListComplimentaryIssuancesByOrg
// ─────────────────────────────────────────────────────────────────────────────

// ListComplimentaryIssuancesByOrg lists all complimentary issuances for an org,
// newest first.
func (q *Queries) ListComplimentaryIssuancesByOrg(
	ctx context.Context,
	orgID uuid.UUID,
) ([]ComplimentaryIssuanceRow, error) {
	const sql = `
SELECT id, org_id, session_id, tier_id, qty, recipients, batch_id, status, issued_by, notes,
       created_at, updated_at
FROM   complimentary_issuances
WHERE  org_id = $1
ORDER BY created_at DESC`
	rows, err := q.db.Query(ctx, sql, orgID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []ComplimentaryIssuanceRow
	for rows.Next() {
		r, err := scanComplimentaryIssuanceRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdateComplimentaryIssuanceStatus
// ─────────────────────────────────────────────────────────────────────────────

// UpdateComplimentaryIssuanceStatus transitions an issuance to a new status.
// Returns pgx.ErrNoRows when the issuance does not exist.
func (q *Queries) UpdateComplimentaryIssuanceStatus(
	ctx context.Context,
	id uuid.UUID,
	newStatus string,
) (ComplimentaryIssuanceRow, error) {
	const sql = `
UPDATE complimentary_issuances
SET    status     = $2,
       updated_at = now()
WHERE  id = $1
RETURNING id, org_id, session_id, tier_id, qty, recipients, batch_id, status, issued_by, notes,
          created_at, updated_at`
	row := q.db.QueryRow(ctx, sql, id, newStatus)
	return scanComplimentaryIssuanceRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertComplimentaryTicket
// ─────────────────────────────────────────────────────────────────────────────

// InsertComplimentaryTicket inserts a ticket row sourced from a complimentary
// issuance batch. Uses complimentary_issuance_id as the source FK (no checkout
// session required). Returns a ComplimentaryTicketRow (not TicketRow) because
// the checkout_session_id column is NULL for complimentary tickets.
func (q *Queries) InsertComplimentaryTicket(
	ctx context.Context,
	complimentaryIssuanceID uuid.UUID,
	sessionID uuid.UUID,
	tierID *uuid.UUID,
	holderEmail *string,
) (ComplimentaryTicketRow, error) {
	const sql = `
INSERT INTO tickets (complimentary_issuance_id, session_id, tier_id, holder_email)
VALUES ($1, $2, $3, $4)
RETURNING id, complimentary_issuance_id, session_id, tier_id, holder_email,
          status, issued_at, created_at, updated_at`
	row := q.db.QueryRow(ctx, sql,
		complimentaryIssuanceID, sessionID, tierID, holderEmail,
	)
	return scanComplimentaryTicketRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListTicketsByComplimentaryIssuance
// ─────────────────────────────────────────────────────────────────────────────

// ListTicketsByComplimentaryIssuance returns all tickets issued for the given
// complimentary issuance, ordered by issuance time then ID. Used as the
// idempotency check before re-inserting tickets.
func (q *Queries) ListTicketsByComplimentaryIssuance(
	ctx context.Context,
	complimentaryIssuanceID uuid.UUID,
) ([]ComplimentaryTicketRow, error) {
	const sql = `
SELECT id, complimentary_issuance_id, session_id, tier_id, holder_email,
       status, issued_at, created_at, updated_at
FROM   tickets
WHERE  complimentary_issuance_id = $1
ORDER BY issued_at ASC, id ASC`
	rows, err := q.db.Query(ctx, sql, complimentaryIssuanceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []ComplimentaryTicketRow
	for rows.Next() {
		r, err := scanComplimentaryTicketRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// HasScannedTicketsForIssuance
// ─────────────────────────────────────────────────────────────────────────────

// HasScannedTicketsForIssuance returns true when at least one ticket belonging
// to the complimentary issuance has a barcode with status='scanned'.
//
// Used in the revocation flow to determine whether clean revocation is possible
// (no scanned tickets) or whether the issuance must be queued for manual review
// (one or more tickets already scanned at the door).
func (q *Queries) HasScannedTicketsForIssuance(
	ctx context.Context,
	complimentaryIssuanceID uuid.UUID,
) (bool, error) {
	const sql = `
SELECT EXISTS (
    SELECT 1
    FROM   barcodes b
    JOIN   tickets  t ON t.id = b.ticket_id
    WHERE  t.complimentary_issuance_id = $1
      AND  b.status = 'scanned'
) AS has_scanned`
	row := q.db.QueryRow(ctx, sql, complimentaryIssuanceID)
	var hasScanned bool
	err := row.Scan(&hasScanned)
	return hasScanned, err
}

// ─────────────────────────────────────────────────────────────────────────────
// RevokeComplimentaryTickets
// ─────────────────────────────────────────────────────────────────────────────

// RevokeComplimentaryTickets transitions all active tickets for the given
// complimentary issuance to 'revoked' status in a single UPDATE statement.
//
// Called within the revocation transaction so that inventory restoration and
// ticket revocation are atomic. Returns the updated rows so the caller can
// iterate over ticket IDs for barcode and credential clean-up.
func (q *Queries) RevokeComplimentaryTickets(
	ctx context.Context,
	complimentaryIssuanceID uuid.UUID,
) ([]ComplimentaryTicketRow, error) {
	const sql = `
UPDATE tickets
SET    status     = 'revoked',
       updated_at = now()
WHERE  complimentary_issuance_id = $1
RETURNING id, complimentary_issuance_id, session_id, tier_id, holder_email,
          status, issued_at, created_at, updated_at`
	rows, err := q.db.Query(ctx, sql, complimentaryIssuanceID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var result []ComplimentaryTicketRow
	for rows.Next() {
		r, err := scanComplimentaryTicketRow(rows)
		if err != nil {
			return nil, err
		}
		result = append(result, r)
	}
	return result, rows.Err()
}
