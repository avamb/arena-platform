// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: payment_intents.sql

package gen

import (
	"context"
	"encoding/json"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// PaymentIntentRow — shared result type for all payment_intents queries
// ─────────────────────────────────────────────────────────────────────────────

// PaymentIntentRow is the result type returned by all payment_intents queries.
//
// State machine: created → requires_action|processing → authorized|succeeded|failed|manual_review.
// Terminal states are succeeded and failed.
//
// SCA fields (sca_redirect_url, client_secret) are non-nil only when the state
// is requires_action and the provider uses a redirect-based 3DS flow.
type PaymentIntentRow struct {
	ID                 uuid.UUID  `json:"id"`
	CheckoutSessionID  *uuid.UUID `json:"checkout_session_id"`
	OrgID              uuid.UUID  `json:"org_id"`
	Provider           string     `json:"provider"`
	ProviderPaymentID  *string    `json:"provider_payment_id"`
	Amount             int64      `json:"amount"`
	Currency           string     `json:"currency"`
	State              string     `json:"state"`
	ScaRedirectURL     *string    `json:"sca_redirect_url"`
	ClientSecret       *string    `json:"client_secret"`
	FailureCode        *string    `json:"failure_code"`
	FailureMessage     *string    `json:"failure_message"`
	AuthorizedAt       *time.Time `json:"authorized_at"`
	SucceededAt        *time.Time `json:"succeeded_at"`
	FailedAt           *time.Time `json:"failed_at"`
	CreatedAt          time.Time  `json:"created_at"`
	UpdatedAt          time.Time  `json:"updated_at"`
}

// scanPaymentIntentRow scans a single payment_intents row into a PaymentIntentRow.
func scanPaymentIntentRow(row interface {
	Scan(dest ...any) error
}) (PaymentIntentRow, error) {
	var r PaymentIntentRow
	err := row.Scan(
		&r.ID,
		&r.CheckoutSessionID,
		&r.OrgID,
		&r.Provider,
		&r.ProviderPaymentID,
		&r.Amount,
		&r.Currency,
		&r.State,
		&r.ScaRedirectURL,
		&r.ClientSecret,
		&r.FailureCode,
		&r.FailureMessage,
		&r.AuthorizedAt,
		&r.SucceededAt,
		&r.FailedAt,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// PaymentIntentEventRow — result type for payment_intent_events queries
// ─────────────────────────────────────────────────────────────────────────────

// PaymentIntentEventRow represents a processed provider webhook event.
// The UNIQUE constraint on (provider_payment_id, event_type) ensures each
// event is recorded and processed exactly once.
type PaymentIntentEventRow struct {
	ID                uuid.UUID       `json:"id"`
	PaymentIntentID   uuid.UUID       `json:"payment_intent_id"`
	ProviderPaymentID string          `json:"provider_payment_id"`
	EventType         string          `json:"event_type"`
	EventPayload      json.RawMessage `json:"event_payload"`
	ResultingState    *string         `json:"resulting_state"`
	ProcessedAt       time.Time       `json:"processed_at"`
}

// scanPaymentIntentEventRow scans a single payment_intent_events row.
func scanPaymentIntentEventRow(row interface {
	Scan(dest ...any) error
}) (PaymentIntentEventRow, error) {
	var r PaymentIntentEventRow
	err := row.Scan(
		&r.ID,
		&r.PaymentIntentID,
		&r.ProviderPaymentID,
		&r.EventType,
		&r.EventPayload,
		&r.ResultingState,
		&r.ProcessedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertPaymentIntent
// ─────────────────────────────────────────────────────────────────────────────

const insertPaymentIntent = `-- name: InsertPaymentIntent :one
INSERT INTO payment_intents (
    checkout_session_id, org_id, provider, provider_payment_id, amount, currency,
    state, sca_redirect_url, client_secret
)
VALUES (
    $1, $2, $3, $4, $5, $6,
    COALESCE(NULLIF($7, ''), 'created'), $8, $9
)
RETURNING id, checkout_session_id, org_id, provider, provider_payment_id, amount, currency,
          state, sca_redirect_url, client_secret, failure_code, failure_message,
          authorized_at, succeeded_at, failed_at, created_at, updated_at`

// InsertPaymentIntent creates a new payment intent linked to an optional checkout session.
//
// Pass checkoutSessionID = nil when creating a standalone payment intent not
// associated with a specific checkout session.
// Pass providerPaymentID = nil when the provider ID is not yet known (it can
// be set later via UpdatePaymentIntentState).
// initialState defaults to 'created' when the empty string is passed.
// scaRedirectURL and clientSecret are optional; pass nil when not applicable.
func (q *Queries) InsertPaymentIntent(
	ctx context.Context,
	checkoutSessionID *uuid.UUID,
	orgID uuid.UUID,
	provider string,
	providerPaymentID *string,
	amount int64,
	currency string,
	initialState string,
	scaRedirectURL *string,
	clientSecret *string,
) (PaymentIntentRow, error) {
	row := q.db.QueryRow(ctx, insertPaymentIntent,
		checkoutSessionID, orgID, provider, providerPaymentID, amount, currency,
		initialState, scaRedirectURL, clientSecret,
	)
	return scanPaymentIntentRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetPaymentIntentByID
// ─────────────────────────────────────────────────────────────────────────────

const getPaymentIntentByID = `-- name: GetPaymentIntentByID :one
SELECT id, checkout_session_id, org_id, provider, provider_payment_id, amount, currency,
       state, sca_redirect_url, client_secret, failure_code, failure_message,
       authorized_at, succeeded_at, failed_at, created_at, updated_at
FROM   payment_intents
WHERE  id = $1`

// GetPaymentIntentByID fetches a payment intent by its UUID primary key.
// Returns pgx.ErrNoRows when not found.
func (q *Queries) GetPaymentIntentByID(ctx context.Context, id uuid.UUID) (PaymentIntentRow, error) {
	row := q.db.QueryRow(ctx, getPaymentIntentByID, id)
	return scanPaymentIntentRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetPaymentIntentByProviderID
// ─────────────────────────────────────────────────────────────────────────────

const getPaymentIntentByProviderID = `-- name: GetPaymentIntentByProviderID :one
SELECT id, checkout_session_id, org_id, provider, provider_payment_id, amount, currency,
       state, sca_redirect_url, client_secret, failure_code, failure_message,
       authorized_at, succeeded_at, failed_at, created_at, updated_at
FROM   payment_intents
WHERE  provider_payment_id = $1`

// GetPaymentIntentByProviderID fetches a payment intent by the provider's
// own payment identifier (e.g. Stripe's pi_… string).
// Used by webhook handlers that receive events keyed by provider ID.
// Returns pgx.ErrNoRows when not found.
func (q *Queries) GetPaymentIntentByProviderID(ctx context.Context, providerPaymentID string) (PaymentIntentRow, error) {
	row := q.db.QueryRow(ctx, getPaymentIntentByProviderID, providerPaymentID)
	return scanPaymentIntentRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListPaymentIntentsByCheckout
// ─────────────────────────────────────────────────────────────────────────────

const listPaymentIntentsByCheckout = `-- name: ListPaymentIntentsByCheckout :many
SELECT id, checkout_session_id, org_id, provider, provider_payment_id, amount, currency,
       state, sca_redirect_url, client_secret, failure_code, failure_message,
       authorized_at, succeeded_at, failed_at, created_at, updated_at
FROM   payment_intents
WHERE  checkout_session_id = $1
ORDER BY created_at DESC, id DESC`

// ListPaymentIntentsByCheckout returns all payment intents linked to the given
// checkout session, newest first. A single checkout session may have multiple
// payment intents when the customer retries after a failure.
func (q *Queries) ListPaymentIntentsByCheckout(ctx context.Context, checkoutSessionID uuid.UUID) ([]PaymentIntentRow, error) {
	rows, err := q.db.Query(ctx, listPaymentIntentsByCheckout, checkoutSessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var intents []PaymentIntentRow
	for rows.Next() {
		r, err := scanPaymentIntentRow(rows)
		if err != nil {
			return nil, err
		}
		intents = append(intents, r)
	}
	return intents, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdatePaymentIntentState
// ─────────────────────────────────────────────────────────────────────────────

const updatePaymentIntentState = `-- name: UpdatePaymentIntentState :one
UPDATE payment_intents
SET    state               = $2,
       updated_at          = now(),
       sca_redirect_url    = COALESCE($3, sca_redirect_url),
       client_secret       = COALESCE($4, client_secret),
       authorized_at       = CASE WHEN $2 = 'authorized' THEN now() ELSE authorized_at END,
       succeeded_at        = CASE WHEN $2 = 'succeeded'  THEN now() ELSE succeeded_at  END,
       failed_at           = CASE WHEN $2 = 'failed'     THEN now() ELSE failed_at     END,
       failure_code        = CASE WHEN $2 = 'failed' THEN COALESCE($5, failure_code)    ELSE failure_code    END,
       failure_message     = CASE WHEN $2 = 'failed' THEN COALESCE($6, failure_message) ELSE failure_message END,
       provider_payment_id = COALESCE(provider_payment_id, $7)
WHERE  id = $1
RETURNING id, checkout_session_id, org_id, provider, provider_payment_id, amount, currency,
          state, sca_redirect_url, client_secret, failure_code, failure_message,
          authorized_at, succeeded_at, failed_at, created_at, updated_at`

// UpdatePaymentIntentState advances a payment intent to a new state.
//
// Optional parameters:
//   - scaRedirectURL: set when transitioning to requires_action to supply the 3DS URL
//   - clientSecret:   set when transitioning to requires_action to supply the SDK secret
//   - failureCode:    set when transitioning to failed for structured error reporting
//   - failureMessage: set when transitioning to failed for human-readable error
//   - providerPaymentID: set on first provider callback if not supplied at creation time
//
// Nil values for optional parameters preserve the existing column values.
// Returns pgx.ErrNoRows when the payment intent does not exist.
func (q *Queries) UpdatePaymentIntentState(
	ctx context.Context,
	id uuid.UUID,
	newState string,
	scaRedirectURL *string,
	clientSecret *string,
	failureCode *string,
	failureMessage *string,
	providerPaymentID *string,
) (PaymentIntentRow, error) {
	row := q.db.QueryRow(ctx, updatePaymentIntentState,
		id, newState, scaRedirectURL, clientSecret, failureCode, failureMessage, providerPaymentID,
	)
	return scanPaymentIntentRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertPaymentIntentEvent
// ─────────────────────────────────────────────────────────────────────────────

const insertPaymentIntentEvent = `-- name: InsertPaymentIntentEvent :one
INSERT INTO payment_intent_events (
    payment_intent_id, provider_payment_id, event_type, event_payload, resulting_state
)
VALUES ($1, $2, $3, $4, $5)
ON CONFLICT (provider_payment_id, event_type) DO NOTHING
RETURNING id, payment_intent_id, provider_payment_id, event_type, event_payload,
          resulting_state, processed_at`

// InsertPaymentIntentEvent records a processed provider webhook event for
// idempotency. Returns pgx.ErrNoRows (and nil event) when the
// (provider_payment_id, event_type) pair already exists in the table — the
// caller should treat this as a duplicate delivery and return 200/204 without
// reprocessing the event.
//
// Pass eventPayload = nil when no raw payload needs to be stored.
// Pass resultingState = nil when the event does not trigger a state transition.
func (q *Queries) InsertPaymentIntentEvent(
	ctx context.Context,
	paymentIntentID uuid.UUID,
	providerPaymentID string,
	eventType string,
	eventPayload []byte,
	resultingState *string,
) (PaymentIntentEventRow, error) {
	row := q.db.QueryRow(ctx, insertPaymentIntentEvent,
		paymentIntentID, providerPaymentID, eventType, eventPayload, resultingState,
	)
	return scanPaymentIntentEventRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetPaymentIntentEvent
// ─────────────────────────────────────────────────────────────────────────────

const getPaymentIntentEvent = `-- name: GetPaymentIntentEvent :one
SELECT id, payment_intent_id, provider_payment_id, event_type, event_payload,
       resulting_state, processed_at
FROM   payment_intent_events
WHERE  provider_payment_id = $1
  AND  event_type          = $2`

// GetPaymentIntentEvent fetches a previously processed webhook event by
// (provider_payment_id, event_type). Returns pgx.ErrNoRows when not found —
// i.e. the event has not been processed yet.
func (q *Queries) GetPaymentIntentEvent(
	ctx context.Context,
	providerPaymentID string,
	eventType string,
) (PaymentIntentEventRow, error) {
	row := q.db.QueryRow(ctx, getPaymentIntentEvent, providerPaymentID, eventType)
	return scanPaymentIntentEventRow(row)
}
