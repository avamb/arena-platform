// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: widget_funnel_events.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// InsertWidgetFunnelEvent
// ─────────────────────────────────────────────────────────────────────────────

const insertWidgetFunnelEvent = `-- name: InsertWidgetFunnelEvent :exec
INSERT INTO widget_funnel_events (
    id,
    feed_token,
    event_type,
    checkout_token,
    session_id,
    occurred_at
) VALUES (
    gen_random_uuid(),
    $1,
    $2,
    $3,
    $4,
    $5
)`

// InsertWidgetFunnelEvent appends one funnel telemetry event to the audit table.
// No PII is stored beyond the opaque checkout_token linkage.
func (q *Queries) InsertWidgetFunnelEvent(ctx context.Context, feedToken, eventType string, checkoutToken *string, sessionID *uuid.UUID, occurredAt time.Time) error {
	_, err := q.db.Exec(ctx, insertWidgetFunnelEvent,
		feedToken,
		eventType,
		checkoutToken,
		sessionID,
		occurredAt,
	)
	return err
}
