// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: webhook_subscribers.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// WebhookSubscriberRow — result type for webhook_subscribers queries
// ─────────────────────────────────────────────────────────────────────────────

// WebhookSubscriberRow is the result type returned by all webhook_subscribers queries.
//
// Feature #156 — WordPress webhook receiver / subscriber registration.
// A subscriber represents an HTTP endpoint (e.g. a WordPress site) that
// receives signed outbox events from the Arena platform.
//
// Fields:
//   - SigningSecret: raw HMAC-SHA256 key. Store and transmit over TLS only.
//   - EventTypes:    empty slice = wildcard (receives all event types).
//   - Active:        soft-delete flag; inactive subscribers are skipped in fan-out.
type WebhookSubscriberRow struct {
	ID            uuid.UUID `json:"id"`
	SiteURL       string    `json:"site_url"`
	CallbackURL   string    `json:"callback_url"`
	SigningSecret string    `json:"signing_secret"`
	EventTypes    []string  `json:"event_types"`
	Active        bool      `json:"active"`
	CreatedAt     time.Time `json:"created_at"`
	UpdatedAt     time.Time `json:"updated_at"`
}

// scanWebhookSubscriberRow scans a single webhook_subscribers row.
func scanWebhookSubscriberRow(row interface {
	Scan(dest ...any) error
}) (WebhookSubscriberRow, error) {
	var r WebhookSubscriberRow
	err := row.Scan(
		&r.ID,
		&r.SiteURL,
		&r.CallbackURL,
		&r.SigningSecret,
		&r.EventTypes,
		&r.Active,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// CreateWebhookSubscriber
// ─────────────────────────────────────────────────────────────────────────────

const createWebhookSubscriberSQL = `-- name: CreateWebhookSubscriber :one
INSERT INTO webhook_subscribers (
    site_url,
    callback_url,
    signing_secret,
    event_types,
    active
) VALUES (
    $1, $2, $3, $4, TRUE
)
RETURNING id, site_url, callback_url, signing_secret, event_types, active, created_at, updated_at`

// CreateWebhookSubscriber registers a new webhook subscriber endpoint.
//
// Parameters:
//   - siteURL:       Human-readable site URL (e.g. "https://mywordpress.example.com").
//   - callbackURL:   The POST endpoint that receives webhook events.
//   - signingSecret: Caller-generated random hex secret (32 bytes recommended).
//   - eventTypes:    List of event_type strings to subscribe to. Empty = wildcard.
//
// Returns the full row including the generated id.
func (q *Queries) CreateWebhookSubscriber(
	ctx context.Context,
	siteURL string,
	callbackURL string,
	signingSecret string,
	eventTypes []string,
) (WebhookSubscriberRow, error) {
	row := q.db.QueryRow(ctx, createWebhookSubscriberSQL,
		siteURL,
		callbackURL,
		signingSecret,
		eventTypes,
	)
	return scanWebhookSubscriberRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListActiveWebhookSubscribers
// ─────────────────────────────────────────────────────────────────────────────

const listActiveWebhookSubscribersSQL = `-- name: ListActiveWebhookSubscribers :many
SELECT id, site_url, callback_url, signing_secret, event_types, active, created_at, updated_at
FROM   webhook_subscribers
WHERE  active = TRUE
ORDER  BY created_at ASC`

// ListActiveWebhookSubscribers returns all active subscribers ordered by creation time.
// Used by the webhook dispatcher to build the fan-out list at runtime.
func (q *Queries) ListActiveWebhookSubscribers(ctx context.Context) ([]WebhookSubscriberRow, error) {
	rows, err := q.db.Query(ctx, listActiveWebhookSubscribersSQL)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var results []WebhookSubscriberRow
	for rows.Next() {
		r, err := scanWebhookSubscriberRow(rows)
		if err != nil {
			return nil, err
		}
		results = append(results, r)
	}
	return results, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// GetWebhookSubscriberByID
// ─────────────────────────────────────────────────────────────────────────────

const getWebhookSubscriberByIDSQL = `-- name: GetWebhookSubscriberByID :one
SELECT id, site_url, callback_url, signing_secret, event_types, active, created_at, updated_at
FROM   webhook_subscribers
WHERE  id = $1`

// GetWebhookSubscriberByID retrieves a single subscriber by primary key.
func (q *Queries) GetWebhookSubscriberByID(ctx context.Context, id uuid.UUID) (WebhookSubscriberRow, error) {
	row := q.db.QueryRow(ctx, getWebhookSubscriberByIDSQL, id)
	return scanWebhookSubscriberRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// DeactivateWebhookSubscriber
// ─────────────────────────────────────────────────────────────────────────────

const deactivateWebhookSubscriberSQL = `-- name: DeactivateWebhookSubscriber :one
UPDATE webhook_subscribers
SET    active     = FALSE,
       updated_at = NOW()
WHERE  id = $1
RETURNING id, site_url, callback_url, signing_secret, event_types, active, created_at, updated_at`

// DeactivateWebhookSubscriber soft-deletes a subscriber by setting active = FALSE.
// Returns the updated row so callers can confirm the deactivation.
func (q *Queries) DeactivateWebhookSubscriber(ctx context.Context, id uuid.UUID) (WebhookSubscriberRow, error) {
	row := q.db.QueryRow(ctx, deactivateWebhookSubscriberSQL, id)
	return scanWebhookSubscriberRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdateWebhookSubscriberEventTypes
// ─────────────────────────────────────────────────────────────────────────────

const updateWebhookSubscriberEventTypesSQL = `-- name: UpdateWebhookSubscriberEventTypes :one
UPDATE webhook_subscribers
SET    event_types = $2,
       updated_at  = NOW()
WHERE  id = $1
RETURNING id, site_url, callback_url, signing_secret, event_types, active, created_at, updated_at`

// UpdateWebhookSubscriberEventTypes replaces the event_types filter for an existing subscriber.
func (q *Queries) UpdateWebhookSubscriberEventTypes(
	ctx context.Context,
	id uuid.UUID,
	eventTypes []string,
) (WebhookSubscriberRow, error) {
	row := q.db.QueryRow(ctx, updateWebhookSubscriberEventTypesSQL, id, eventTypes)
	return scanWebhookSubscriberRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// SetWebhookSubscriberActive
// ─────────────────────────────────────────────────────────────────────────────

const setWebhookSubscriberActiveSQL = `-- name: SetWebhookSubscriberActive :one
UPDATE webhook_subscribers
SET    active     = $2,
       updated_at = NOW()
WHERE  id = $1
RETURNING id, site_url, callback_url, signing_secret, event_types, active, created_at, updated_at`

// SetWebhookSubscriberActive flips the active flag of an existing subscriber.
//
// Backs the SuperAdmin webhooks admin UI (Feature #294 — S-3 "Webhook
// subscribers admin UI"). Used by PATCH /v1/webhooks/subscribers/{id}
// so an operator can re-activate a previously soft-deleted subscriber
// or deactivate one without an explicit DELETE. The dedicated
// DeactivateWebhookSubscriber query is retained as the canonical
// soft-delete invoked by the DELETE handler so the two flows remain
// distinguishable in the audit/log surface.
func (q *Queries) SetWebhookSubscriberActive(
	ctx context.Context,
	id uuid.UUID,
	active bool,
) (WebhookSubscriberRow, error) {
	row := q.db.QueryRow(ctx, setWebhookSubscriberActiveSQL, id, active)
	return scanWebhookSubscriberRow(row)
}
