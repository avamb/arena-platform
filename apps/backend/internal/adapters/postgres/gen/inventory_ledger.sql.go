// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: inventory_ledger.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

// ─────────────────────────────────────────────────────────────────────────────
// InventoryLedgerRow — shared result type for all inventory_ledger queries
// ─────────────────────────────────────────────────────────────────────────────

// InventoryLedgerRow is the result type returned by all inventory_ledger queries.
//
// tier_id is nil for the session-level aggregate row and non-nil for
// tier-scoped rows.
//
// capacity_total nil means unlimited availability; capacity checks are skipped.
//
// version is a monotonically incrementing counter. It is incremented on every
// write so callers can detect concurrent modification by comparing versions.
//
// Capacity invariant (always holds when capacity_total IS NOT NULL):
//
//	capacity_held + capacity_sold <= capacity_total
type InventoryLedgerRow struct {
	ID            uuid.UUID  `json:"id"`
	SessionID     uuid.UUID  `json:"session_id"`
	TierID        *uuid.UUID `json:"tier_id"`     // nil = session-level aggregate
	CapacityTotal *int32     `json:"capacity_total"` // nil = unlimited
	CapacityHeld  int32      `json:"capacity_held"`  // reserved, not confirmed
	CapacitySold  int32      `json:"capacity_sold"`  // confirmed (sold)
	Version       int64      `json:"version"`
	CreatedAt     time.Time  `json:"created_at"`
	UpdatedAt     time.Time  `json:"updated_at"`
}

// scanInventoryLedgerRow scans a single inventory_ledger row into an InventoryLedgerRow.
func scanInventoryLedgerRow(row interface {
	Scan(dest ...any) error
}) (InventoryLedgerRow, error) {
	var r InventoryLedgerRow
	err := row.Scan(
		&r.ID,
		&r.SessionID,
		&r.TierID,
		&r.CapacityTotal,
		&r.CapacityHeld,
		&r.CapacitySold,
		&r.Version,
		&r.CreatedAt,
		&r.UpdatedAt,
	)
	return r, err
}

// ─────────────────────────────────────────────────────────────────────────────
// InsertInventoryLedger
// ─────────────────────────────────────────────────────────────────────────────

const insertInventoryLedger = `-- name: InsertInventoryLedger :one
INSERT INTO inventory_ledger (session_id, tier_id, capacity_total)
VALUES ($1, $2, $3)
RETURNING id, session_id, tier_id, capacity_total, capacity_held, capacity_sold,
          version, created_at, updated_at`

// InsertInventoryLedger creates a new inventory ledger row for a session or tier.
// Pass tierID = nil for the session-level aggregate row.
// Pass capacityTotal = nil for unlimited availability.
// Returns the created row including the uuidv7 PK assigned by the database.
func (q *Queries) InsertInventoryLedger(
	ctx context.Context,
	sessionID uuid.UUID,
	tierID *uuid.UUID,
	capacityTotal *int32,
) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, insertInventoryLedger, sessionID, tierID, capacityTotal)
	return scanInventoryLedgerRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// GetInventoryLedger
// ─────────────────────────────────────────────────────────────────────────────

const getInventoryLedger = `-- name: GetInventoryLedger :one
SELECT id, session_id, tier_id, capacity_total, capacity_held, capacity_sold,
       version, created_at, updated_at
FROM   inventory_ledger
WHERE  session_id = $1
  AND  (tier_id = $2::uuid OR (tier_id IS NULL AND $2::uuid IS NULL))`

// GetInventoryLedger fetches the ledger row for a (session, tier) pair.
// Pass tierID = nil to fetch the session-level aggregate row.
// Returns pgx.ErrNoRows when no matching row exists.
func (q *Queries) GetInventoryLedger(ctx context.Context, sessionID uuid.UUID, tierID *uuid.UUID) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, getInventoryLedger, sessionID, tierID)
	return scanInventoryLedgerRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ListInventoryLedgersBySession
// ─────────────────────────────────────────────────────────────────────────────

const listInventoryLedgersBySession = `-- name: ListInventoryLedgersBySession :many
SELECT id, session_id, tier_id, capacity_total, capacity_held, capacity_sold,
       version, created_at, updated_at
FROM   inventory_ledger
WHERE  session_id = $1
ORDER BY tier_id NULLS FIRST, id ASC`

// ListInventoryLedgersBySession returns all ledger rows for the given session.
// The session-level aggregate row (tier_id IS NULL) is returned first, followed
// by tier-level rows ordered by id.
func (q *Queries) ListInventoryLedgersBySession(ctx context.Context, sessionID uuid.UUID) ([]InventoryLedgerRow, error) {
	rows, err := q.db.Query(ctx, listInventoryLedgersBySession, sessionID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()

	var ledgers []InventoryLedgerRow
	for rows.Next() {
		r, err := scanInventoryLedgerRow(rows)
		if err != nil {
			return nil, err
		}
		ledgers = append(ledgers, r)
	}
	return ledgers, rows.Err()
}

// ─────────────────────────────────────────────────────────────────────────────
// ReserveCapacity
// ─────────────────────────────────────────────────────────────────────────────

const reserveCapacity = `-- name: ReserveCapacity :one
WITH locked AS (
    SELECT id, capacity_total, capacity_held, capacity_sold
    FROM   inventory_ledger
    WHERE  session_id = $1
      AND  (tier_id = $2::uuid OR (tier_id IS NULL AND $2::uuid IS NULL))
    FOR UPDATE
)
UPDATE inventory_ledger il
SET    capacity_held = il.capacity_held + $3::integer,
       version       = il.version + 1,
       updated_at    = now()
FROM   locked
WHERE  il.id = locked.id
  AND  (locked.capacity_total IS NULL
        OR locked.capacity_held + locked.capacity_sold + $3::integer <= locked.capacity_total)
RETURNING il.id, il.session_id, il.tier_id, il.capacity_total,
          il.capacity_held, il.capacity_sold, il.version, il.created_at, il.updated_at`

// ReserveCapacity atomically reserves `amount` units of capacity.
//
// Uses SELECT FOR UPDATE inside a CTE to serialise concurrent operations.
// Returns the updated row on success.
// Returns pgx.ErrNoRows when:
//   - the row does not exist for (sessionID, tierID), OR
//   - capacity_held + capacity_sold + amount > capacity_total (over-capacity).
//
// The caller should surface ErrNoRows as HTTP 409 Conflict with code
// "inventory.over_capacity".
func (q *Queries) ReserveCapacity(ctx context.Context, sessionID uuid.UUID, tierID *uuid.UUID, amount int32) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, reserveCapacity, sessionID, tierID, amount)
	return scanInventoryLedgerRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ReleaseCapacity
// ─────────────────────────────────────────────────────────────────────────────

const releaseCapacity = `-- name: ReleaseCapacity :one
WITH locked AS (
    SELECT id, capacity_held
    FROM   inventory_ledger
    WHERE  session_id = $1
      AND  (tier_id = $2::uuid OR (tier_id IS NULL AND $2::uuid IS NULL))
    FOR UPDATE
)
UPDATE inventory_ledger il
SET    capacity_held = il.capacity_held - $3::integer,
       version       = il.version + 1,
       updated_at    = now()
FROM   locked
WHERE  il.id = locked.id
  AND  locked.capacity_held >= $3::integer
RETURNING il.id, il.session_id, il.tier_id, il.capacity_total,
          il.capacity_held, il.capacity_sold, il.version, il.created_at, il.updated_at`

// ReleaseCapacity releases `amount` units from held back to available.
// Returns pgx.ErrNoRows when held < amount (cannot release more than held).
func (q *Queries) ReleaseCapacity(ctx context.Context, sessionID uuid.UUID, tierID *uuid.UUID, amount int32) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, releaseCapacity, sessionID, tierID, amount)
	return scanInventoryLedgerRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// ConfirmCapacity
// ─────────────────────────────────────────────────────────────────────────────

const confirmCapacity = `-- name: ConfirmCapacity :one
WITH locked AS (
    SELECT id, capacity_held
    FROM   inventory_ledger
    WHERE  session_id = $1
      AND  (tier_id = $2::uuid OR (tier_id IS NULL AND $2::uuid IS NULL))
    FOR UPDATE
)
UPDATE inventory_ledger il
SET    capacity_held = il.capacity_held - $3::integer,
       capacity_sold = il.capacity_sold + $3::integer,
       version       = il.version + 1,
       updated_at    = now()
FROM   locked
WHERE  il.id = locked.id
  AND  locked.capacity_held >= $3::integer
RETURNING il.id, il.session_id, il.tier_id, il.capacity_total,
          il.capacity_held, il.capacity_sold, il.version, il.created_at, il.updated_at`

// ConfirmCapacity moves `amount` from held to sold (purchase confirmed).
// Returns pgx.ErrNoRows when held < amount.
func (q *Queries) ConfirmCapacity(ctx context.Context, sessionID uuid.UUID, tierID *uuid.UUID, amount int32) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, confirmCapacity, sessionID, tierID, amount)
	return scanInventoryLedgerRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// RestoreSoldCapacity
// ─────────────────────────────────────────────────────────────────────────────

const restoreSoldCapacity = `-- name: RestoreSoldCapacity :one
WITH locked AS (
    SELECT id, capacity_sold
    FROM   inventory_ledger
    WHERE  session_id = $1
      AND  (tier_id = $2::uuid OR (tier_id IS NULL AND $2::uuid IS NULL))
    FOR UPDATE
)
UPDATE inventory_ledger il
SET    capacity_sold = il.capacity_sold - $3::integer,
       version       = il.version + 1,
       updated_at    = now()
FROM   locked
WHERE  il.id = locked.id
  AND  locked.capacity_sold >= $3::integer
RETURNING il.id, il.session_id, il.tier_id, il.capacity_total,
          il.capacity_held, il.capacity_sold, il.version, il.created_at, il.updated_at`

// RestoreSoldCapacity decrements capacity_sold by `amount`, moving units back
// to available. This is the inverse of ConfirmCapacity.
//
// Used when a complimentary issuance is revoked so the freed capacity becomes
// available for new reservations. Runs inside the revocation transaction.
//
// Returns pgx.ErrNoRows when:
//   - the ledger row does not exist, OR
//   - capacity_sold < amount (cannot restore more than was confirmed).
func (q *Queries) RestoreSoldCapacity(ctx context.Context, sessionID uuid.UUID, tierID *uuid.UUID, amount int32) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, restoreSoldCapacity, sessionID, tierID, amount)
	return scanInventoryLedgerRow(row)
}

// ─────────────────────────────────────────────────────────────────────────────
// UpdateCapacityTotal
// ─────────────────────────────────────────────────────────────────────────────

const updateCapacityTotal = `-- name: UpdateCapacityTotal :one
UPDATE inventory_ledger
SET    capacity_total = $3::integer,
       version        = version + 1,
       updated_at     = now()
WHERE  session_id = $1
  AND  (tier_id = $2::uuid OR (tier_id IS NULL AND $2::uuid IS NULL))
  AND  ($3::integer IS NULL OR $3::integer >= capacity_held + capacity_sold)
RETURNING id, session_id, tier_id, capacity_total, capacity_held, capacity_sold,
          version, created_at, updated_at`

// UpdateCapacityTotal propagates a capacity_total change from a session or tier.
// Pass newTotal = nil to remove the capacity ceiling (unlimited).
// Returns pgx.ErrNoRows when:
//   - no matching row exists for (sessionID, tierID), OR
//   - newTotal < capacity_held + capacity_sold (invariant would be violated).
func (q *Queries) UpdateCapacityTotal(ctx context.Context, sessionID uuid.UUID, tierID *uuid.UUID, newTotal *int32) (InventoryLedgerRow, error) {
	row := q.db.QueryRow(ctx, updateCapacityTotal, sessionID, tierID, newTotal)
	return scanInventoryLedgerRow(row)
}
