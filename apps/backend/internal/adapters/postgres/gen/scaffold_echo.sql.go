// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v2.3.0
// source: scaffold_echo.sql

package gen

import (
	"context"
	"time"

	"github.com/google/uuid"
)

const insertScaffoldEcho = `-- name: InsertScaffoldEcho :one
INSERT INTO scaffold_echo (actor_id, message)
VALUES ($1::uuid, $2)
RETURNING id, actor_id, message, created_at
`

// InsertScaffoldEchoRow is the result type for InsertScaffoldEcho.
type InsertScaffoldEchoRow struct {
	ID        uuid.UUID `json:"id"`
	ActorID   uuid.UUID `json:"actor_id"`
	Message   string    `json:"message"`
	CreatedAt time.Time `json:"created_at"`
}

// InsertScaffoldEcho inserts a new scaffold_echo row and returns its generated
// id, the actor_id (as uuid), message, and created_at timestamp.
//
// This is the foundation milestone's example of a typed sqlc query that
// participates in a cross-cutting transaction (auth → permission → idempotency
// → audit → outbox → scaffold_echo INSERT → COMMIT). Remove when real domain
// queries exist.
func (q *Queries) InsertScaffoldEcho(ctx context.Context, actorID string, message string) (InsertScaffoldEchoRow, error) {
	row := q.db.QueryRow(ctx, insertScaffoldEcho, actorID, message)
	var i InsertScaffoldEchoRow
	err := row.Scan(
		&i.ID,
		&i.ActorID,
		&i.Message,
		&i.CreatedAt,
	)
	return i, err
}
