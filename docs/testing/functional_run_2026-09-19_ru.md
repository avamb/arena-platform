# Функциональный прогон arenasoldout.com + Arena — 19.09.2026

Цель: пройти все процессы вручную (ивент-центр сайта, админка Arena под супер-админом,
площадки с часовыми поясами, покупки, возвраты, MACS) и собрать мелкие баги. Без нагрузки.

Стенд: Arena `6288b94` → `01d692d` → `483d900` на arena-prod-1 (api.arenasoldout.com, app.arenasoldout.com),
магазин arenasoldout.com (main `1b5dbfe`), Stripe в тестовом режиме, EUR.

## Подготовка

- Arena `6288b94` выкачена (api/worker healthy, migrate exit 0, лог `commit=6288b94`), админка пересобрана.
- CI первого прогона упал на `TestW1A5a_ConcurrentExtend_SameSeats_LiveDB` — ошибка
  самого PostgreSQL, см. F-1; перезапуск зелёный.
- Магазин: тестовый шлюз `aso-test-gateway.php` (2cbd991) — оплата настоящим Stripe
  PaymentIntent в тестовом режиме токеном `pm_card_visa` без ввода карты; виден только
  админу после `?aso_test_gw=1`; `+decline` / `+funds` в email — отказ банка.
- Ивент-центр: вкладка Arena была мёртвой («Модуль arena недоступен») — `class-lops-arena.php`
  не был скопирован с ветки lampyris `feat/arena-events-tab`; перенесён (1b5dbfe).
- Тестовые данные с нуля: организация **Arena Test Promotions** (#4,
  `01a0b8ec-fa77-77d1-9bd4-c47f7f93e4fe`, CZ, en), афиши 640×670 в медиатеке (12798, 12803, 12808).

## Находки

Статус: 🔴 баг, 🟠 недочёт/UX, 🔵 улучшение, ✅ проверено и работает.

### Инфраструктура / CI
- **F-1 🔴** CI Integration: `bump seat_status_version: ERROR: new multixact has more than one
  updating member (SQLSTATE XX000)` в `TestW1A5a_ConcurrentExtend_SameSeats_LiveDB` — внутренняя
  ошибка PostgreSQL при конкурентных `FOR NO KEY UPDATE`/`FOR KEY SHARE` на строке `sessions`.
  Локально (PG 17.10) 10/10 зелёный, в CI образ `postgres:17` (плавающий минор). Нужно:
  зафиксировать минорную версию в CI, сверить версию PG на проде, повторять транзакцию при XX000
  с этим текстом либо исследовать, какой минор это вносит. Если прод на том же миноре — продление
  брони может падать у реальных покупателей.

### Ивент-центр (сайт)
- **F-2 🔴** Вкладка Arena не работала — нет `class-lops-arena.php` (исправлено 1b5dbfe).
- **F-3 🟠** В копии lampyris-ops на arenasoldout нет туров, промоутеров (`Lops_Promoters`,
  `mu-plugins/lampyris-promoters.php`, `lampyris-tours.php`) — тесты t1/t2/t3/c12 красные.
- **F-4 🟠** Лексика: в ивент-центре «товар» вместо «ивент» (просьба владельца — править попутно).
- ✅ Мероприятие из ивент-центра «Baltic Strings — Tallinn» (новая площадка, Europe/Tallinn,
  3 категории, афиша): в Arena площадка не задублирована, 19:00 Таллина = 17:00 UTC, категории и
  ёмкость верны; синк создал товар с афишей; покупка #12898 (2× Premium, 90,90 € со сбором 1%) →
  письмо, MACS получил оба билета с `showTime 19:00` (вебхук MACS org #4 работает).
- ✅ Правка через ивент-центр: время 19:30, VIP 90→95, новая категория Balcony — Arena применила,
  проданные 2 места сохранены, сайт получил новую вариацию и цены.
- **F-46 🔴 (исправлено arenasoldout 7134571)** Каждый синк каталога заново скачивал афишу: Arena
  подписывает URL `/v1/media-files/<id>?expires=…&sig=…` при каждом чтении, плагин сравнивал URL
  целиком. На каждый прогон — новое вложение плюс 4 копии WPML (15 вложений у одного товара за
  три синка). Теперь сравнивается путь без подписи. Старые дубли (≈60 вложений) остались — чистить
  по решению владельца. **То же нужно перенести в Lampyris/Vino&Co перед переводом** (часовой крон).
- **F-47 🔴 (исправлено)** Ключ ивент-центра нельзя было привязать к каналу из админки (в форме
  нет поля, в списке не видно) → «publish» из ивент-центра не публиковал мероприятие в канал сайта
  (`import.channel_publication_skipped`), вебхук `event.created` сайту не уходил. Хуже: сервер
  принимал `channel_id` чужой организации — ключ org A мог публиковать в витрину org B. Теперь
  422 `api_key.invalid_channel`, в админке выбор канала и колонка Channel. Действующий ключ
  без канала надо перевыпустить с каналом #4 (секрет вводит владелец).
- **F-48 🟠** Названия вариаций «… - Premium, -» — висящий «, -» от пустого второго атрибута.
- **F-49 🟠** Перенос времени сеанса после продаж (19:00→19:30) не доходит до MACS (там осталось
  19:00) и покупателям не сообщается. Нужна политика: уведомление/обновление MACS или запрет.
- **F-50 🟢** В форме покупки на сайте телефон по умолчанию `+420` и для таллинского мероприятия.
- ✅ Второе мероприятие из ивент-центра «Riga Organ Evening»: новые страна (LV), город Riga и площадка
  созданы на лету, пояс Europe/Riga, 18:00 Риги = 16:00 UTC, адрес с диакритикой дошёл до сайта.
- **F-51 🟠 (исправлено)** Адрес площадки, созданной из ивент-центра, писался только в старую колонку
  `venues.address`; форма площадки в админке показывает `address_line1` → адрес выглядел пустым.
  Импорт теперь пишет и `address_line1`. Уже созданные площадки (Test Hall Riga) — поправить руками.
- **F-28 🔴 (исправлено arenasoldout 024a645)** Бесплатная категория: модалка считала цену 0
  «неизвестной» и молча не пускала дальше (`modal-booking.js`), а оформление отправило бы заказ на 0 €
  на страницу оплаты Stripe. Теперь цена 0 допустима, а заказ завершается без оплаты, только если
  платформа подтвердила итог 0 (`bil24_order_total_sum`).
- ✅ Бесплатный билет Kids (0 €) на «Riga Organ Evening» после исправления: заказ #12913 Completed без
  шага оплаты, PAY_ORDER на 0, PDF через Brevo, MACS принял билет (18:00 Риги).
- **F-52 🔴 (исправлено arenasoldout 4ee1846)** Бесплатный заказ завершается внутри AJAX
  `vino_modal_checkout`, а `bil24-ajax-optimize.php` грузит там только белый список плагинов без
  почтовика билетов → платформа слала второй экземпляр билетов (SEND_TICKETS_TO_EMAIL не подавлен),
  а письма WooCommerce падали («Could not instantiate mail function» — Brevo-маршрут без ключа).
  Почтовик добавлен в белый список. **Касается и Lampyris/Vino при бесплатных категориях.**
- **F-53 🔴 (исправлено)** Количество категории, изменённое в ивент-центре, Arena молча игнорировала
  (правило «повторный импорт не меняет количество» из плана GA-квот писалось для выгрузок Bil24, где
  availability — остаток). Сайт показывал новое число, платформа продавала по старому. Теперь пакет
  ивент-центра применяет количество через квоты; ниже занятого — предупреждение
  `import.category_quantity_below_used`, прежнее количество сохраняется.
- **F-54 🟠 (исправлено)** Нестабильный CI-тест `TestInfoP99Latency_FullVerification/Step7`: отношение
  p99 12 мс / 1 мс на общем раннере валило прогон a4946d5 без проблем с логированием.
- ✅ F-53 на стенде (ad934b0): Standard 200→250 из ивент-центра применилось (вместимость 400),
  Premium 80→1 при 2 проданных — отказ с предупреждением, осталось 80. Предупреждения ивент-центр
  теперь поясняет по-русски (arenasoldout 41757e5).
- **F-55 🟢** Отмена бесплатного билета из ивент-центра (#12913) работает до MACS, но заказ остаётся
  «Completed», хотя все его билеты аннулированы (у платного полный возврат даёт «Refunded»).
- **F-56 🟠** После сохранения с отказом по количеству форма ивент-центра показывает введённое (1),
  а на платформе 80 — при следующем сохранении предупреждение повторится.
- **F-57 🔴 (исправлено ad934b0)** Интеграционный тест 533 и харнесс `04_refund_dedup` крали друг у
  друга строки общей `outbox_events` в параллельном CI (no-op диспетчер MACS «доставлял» чужие
  события). 533 теперь вычерпывает только свои строки; правило в AGENTS.md.
- ✅ Места на Berlin Chamber Night (канал #4): #12917, 2 места, 78,78 €, MACS «Parter / ряд 2 /
  места 2, 3». F-43 (цвета) подтверждается и здесь — платформа отдаёт `sbt:color` #eeeeee/#111111,
  хотя SVG загружалась с #e53935/#1e88e5.
- **F-43 — причина найдена и исправлена.** `seating/svg_import.go`: регулярка `#([0-9a-f]{3}|[0-9a-f]{6})`
  в RE2 берёт первую альтернативу, из `#e53935` вырезалось `e53` и раздувалось до `#ee5533`;
  нормализация повторяется при импорте, Canonicalize и привязке сеанса → `#eeeeee`. Порядок
  альтернатив исправлен, тест идемпотентности, эталон Palac Akropolis пересобран (в нём был
  зашит баг: красный `#ff0000` хранился как `#ffffff`). **Уже импортированные схемы хранят
  испорченные цвета без возможности восстановления — переимпортировать из исходных SVG**
  (тестовая Berlin, всё, что заходило импортом на staging Lampyris).
- ✅ F-43 на стенде (8c253f9): новая схема «Test Club Berlin — Colour check», сеанс 22.11 на Berlin
  Chamber Night → на сайте (#12920) `Parter=#e53935` ×50, `Balcony=#1e88e5` ×16, легенда совпадает.
- ✅ Отмена бесплатного билета из ивент-центра (#12913): Arena → `ticket.refunded` → сверка → MACS
  «возвращён», статус «⚪ возвращён».
- **F-58 🟠** Таблица категорий сеанса со схемой: «ПРОДАНО 0» и «СВОБОДНО —» у Parter первого
  сеанса Berlin, хотя там продано 2 места (#12917) — счётчики не учитывают места схемы.
- 🔵 Стенд: PostgreSQL 17.11 (к F-1: в CI плавающий `postgres:17`).
- **F-34 — исправлено частично (arenasoldout 8480066).** «Активные билеты» считались как «продано −
  возвращено», поэтому билеты, аннулированные на платформе без возврата денег, и заказы, отменённые
  окном оплаты, числились активными. Теперь считаются по статусу, в разбивке есть «Отменены: оплата
  не подтвердилась вовремя». ASO Test: 5 активных (было 7), разбивка 5 / 2 / 2 сходится с заказами.
  Выручка/нетто по-прежнему включают #12813 — он возвращён прямо в WooCommerce мимо ивент-центра
  (артефакт F-20/F-21); это состояние подсвечивает вкладка «Возвраты», учёт денег не менял.
- **F-59 🟠** Ключ ивент-центра: повторное сохранение того же ключа не привязывает его к каналу —
  привязка хранится в ключе на платформе. Нужен новый ключ с выбранным каналом (F-47).
- **F-60 🟢** Сводка: дата сеанса сырой строкой «2026-10-31T20:00:00»; у Baltic Strings показано
  19:00, хотя сеанс перенесён на 19:30 (берётся из билетов заказов, а не из каталога).

### Покупки и возвраты (сайт ↔ Arena ↔ MACS)
- **F-20 🔴 (исправлено 22d553b)** После успешного PAY_ORDER `bil24-payment-window.php`
  (`on_pay_result`) всегда шёл в «захват Stripe» и, не найдя `_stripe_charge_captured`, отменял
  билеты в Arena и заказ. Задевает любой заказ не через Stripe с отложенным захватом —
  **бесплатные (0 €) заказы и любой другой шлюз, в т.ч. на Lampyris/Vino**. Исправлено: для
  не-Stripe заказа — сразу успех без захвата.
- **F-21 🔴 (исправлено 22d553b)** Поздний `order.paid` (поставлен в очередь тем же PAY_ORDER)
  возвращал отменённый сайтом заказ в «Processing» и отдавал отменённые билеты почтовику.
  Теперь игнорируется, если сайт уже отменил билеты (`bil24_pw_tickets_cancelled`).
- **F-22 🔴 (исправлено f0bcc39, 96dc9ff)** Возврат билета из ивент-центра возвращал деньги, но
  билет оставался активным в Arena и MACS («Awaiting Bil24 cancellation») — **возвращённый
  билет продолжал пускать на вход**. Теперь на сайте с Arena сразу REFUND_TICKET → Arena
  отменяет → MACS → `ticket.refunded` закрывает запись. Ложное «amount mismatch … refundPrice 0»
  на каждом возврате убрано. Для Lampyris на Bil24 поведение прежнее. Тест `test-a2`.
- **F-23 🟠** Отказ банка: заказ «Failed» и бронь снята сразу, а страница оплаты продолжает
  показывать «Booking held for another 19:50»; на один отказ — три письма «Failed order».
  (Сверить с настоящим Stripe — часть может быть от тестового шлюза.)
- **F-24 🟠** Страница оплаты: Stripe Link показывает сохранённую карту, но «Pay» без «Confirm»
  отвечает «Please select a payment method» — неочевидно.
- **F-25 🟠** Страница мероприятия (arenasoldout): нет даты, площадки, описания, картинки;
  две кнопки «Buy ticket»; количество на странице не переходит в окно покупки (там снова 1);
  в окне «Ticket · 50 €» без названия категории.
- **F-26 🟠** Ивент-центр: email с «+» выводится с пробелом («andreev buyer2@…»); тексты
  «Bil24 № …», «Awaiting Bil24 cancellation», «Обновляется автоматически из Bil24», «из Bil24» на
  сайте с Arena; письмо BTM без messageId Brevo;
  в `ticket.refunded` из админки Arena нет даты («date ?»).
- **F-27 🔵** Почтовик билетов на staging пропускает только белый список — был только
  ava@abhteam.com; добавлены адреса владельца для тестов.
- ✅ Покупка #12815 (1× Standart) и #12817 (3× VIP): оплата → PAY_ORDER → «Completed» → PDF →
  письмо через Brevo (ambamain@) → MACS принял все билеты (200). Отказ карты (#12816) — заказ
  Failed, бронь снята. Отмена билета из админки Arena доходит до MACS и сайта.
- **F-28 🔴** Бесплатную категорию (free, 0 €), созданную в Arena, сайт продать не может: окно
  покупки (`vino-purchase-flow-ux/js/modal-booking.js:264`, `resolvedPrice <= 0`) отбивает её
  без видимой ошибки — просто краснеет «Next». Нужно решить: бесплатные билеты только через
  приглашения ивент-центра или поддержать 0 € в окне покупки (после F-20 бэкенд-путь готов).
- **F-29 🟠** Периодическая синхронизация каталога на сайте выключена (`cron_enabled=no`), и Arena не
  шлёт сайту «каталог изменился» — мероприятия/категории, созданные в админке Arena, появляются
  на сайте только после ручного «Sync» (ивент-центр сам запускает синхронизацию).
- **F-30 🔵** Выгрузка MACS: `charge` = сумма к оплате после скидки (как в старом формате Bil24),
  а в вебхуке `order.paid` `charge` = сервисный сбор (0). Один ключ — два смысла; суммы заказа в
  выгрузке «только действующие» не пересчитаны (1000000991: sum 150 при одном живом билете).
- **F-31 🟠** Билет, отменённый в админке Arena с решением «none», не получает даты
  возврата/отмены: в CSV отменённых пустая дата, на сайте «date ?».
- **F-34 🔴** «Сводка» ивент-центра: «Активные билеты 6» при 4 реальных, «Выручка 310 / Возвращено
  100 / Нетто 210» при реальных 110 — заказ, возвращённый целиком через WooCommerce (#12813, 100 €),
  не попал в «Возвращено», а его отменённые билеты считаются активными. Дата сырой строкой
  «2026-10-31T20:00:00».
- **F-35 🟠** Админка Arena: «Refunds» пуст, хотя возвраты были (реестр видит только возвраты,
  запрошенные через Arena, а не отмены через шлюз); «Orders» показывает UUID вместо номера
  заказа (1000000991) и организации — искать заказ по номеру с сайта нельзя.
- **F-36 🟠** Пригласительный (0 € на сайте) в Arena записан как оплаченный на полную цену
  (`order.paid sum=20 totalSum=20`) — отчёты Arena по выручке завышены на приглашения. Окно покупки
  в режиме пригласительного показывает «Ticket · 20 €» и «PAY NOW».
- **F-37 🔵** Плагин окна оплаты с багами F-20/F-21 пришёл в arenasoldout с ветки Lampyris
  `staging/payment-window-on-main`; на `main` (прод Lampyris) его нет. Перенести исправления в эту
  ветку до перевода Lampyris на Arena.
- ✅ Пригласительный из ивент-центра (#12820, `lops_invite`, 0 €): заказ «Completed», билет выписан —
  до исправления F-20 он был бы отменён.
- ✅ Выгрузка «MACS export (valid tickets)» — 4 заказа, только живые билеты (из 1000000991 — один
  билет 14); «Revoked tickets (CSV)» — 11, 12, 15, 16; счётчик «Valid: 4 · Revoked: 4» верный.
- MACS (к этапу доработки MACS): в логе «ОШИБКА при добавлении barcodeFormat: 'dict' object has
  no attribute 'id'» на каждом билете — безвредно (есть фолбэк), но шумит.

### Админка Arena
- **F-5 🟠** Список площадок: колонки «Организация» и «Город» показывают UUID, а не названия;
  нет колонки часового пояса; у «Palac Akropolis #1» пустая страна.
- **F-6 🔵** Новая площадка: после выбора адреса (город Прага, координаты) часовой пояс не
  подставляется — можно выводить из страны/города/координат.
- **F-7 🟠** Форма площадки принимает `Mars/Olympus` без локальной ошибки; сервер отвечает
  «timezone must be a known IANA zone name» — можно проверять по списку `Intl.supportedValuesOf`.
- **F-8 🟠** Страна в новой площадке предзаполнена страной организации (Estonia) даже при
  выборе адреса — адрес потом правит её на CZ, ок; но без адреса легко оставить чужую страну.
- **F-9 🟠** Смешанный язык интерфейса при выбранном «Русский»: дашборд, «Users», «Customers»,
  «Orders» (два пункта «Orders»/«Заказы»), «Webhook Subscribers», «Customer Imports», все
  описания страниц на английском; устаревшие тексты «Shell only -- backend contract gap».
- **F-10 🔵** Создание организации: slug не генерируется из названия; поиск страны по названию
  («Czech») не находит — работает только ISO-код.
- **F-8a 🔴** Если поиск адреса ничего не нашёл, форма всё равно создаёт площадку со страной
  организации (CZ для таллинской площадки), без города и адреса — ни предупреждения, ни
  обязательности. Подсказка «Prefilled from the owning organization (CZ)» остаётся и после
  смены страны на DE.
- **F-11 🟠** Список мероприятий: колонка «Channels» всегда «—», даже у мероприятий,
  опубликованных в канал и продающихся через него.
- **F-12 🔴** Мастер создания мероприятия: «Additional dates» создаёт сеансы без категорий,
  а шаг категорий есть только для первого сеанса → «Cannot publish: every session must have at
  least one ticket tier», из мастера не выйти в публикацию. Обход — вкладка «Ticket tiers» в
  карточке мероприятия (там добавление по каждому сеансу работает). Нужно: копировать
  категории на все даты или дать шаг категорий по каждой дате.
- **F-13 🟠** Публикация в канал: «No venue city on the event's sessions — defaulting to global»,
  хотя у площадки город указан (Prague) — выбор города подставляется не из площадки.
- **F-14 🟠** В «Edit» мероприятия нет афиши/возраста/полного названия — только название,
  видимость, описание. Афиша загружается, но на вкладке «Sessions» по каждому сеансу — найти
  трудно.
- **F-33 🔴 (исправлено 483d900)** Создание пользователя и приглашение в организацию из админки не
  отправляют письма: `hiam/admin_users.go`/`admin_memberships.go` только пишут
  «EMAIL DELIVERY (dev-mode)» в лог — **вместе с полной ссылкой и токеном установки пароля**.
  Ссылка к тому же `http://api.arenasoldout.com/v1/auth/password-reset/confirm?…` — http и
  POST-эндпоинт API, а не страница админки. Организаторы не могут войти. Админка при этом
  пишет «A password setup link has been issued to the email address». Вдобавок токен хранился
  в открытом виде, а подтверждение ищет его SHA-256 — ссылка не сработала бы никогда. Теперь письмо
  идёт той же задачей воркера, что и сброс пароля (`auth.password_reset_email`, purpose
  `account_setup`/`org_invitation`), ссылка `https://app.arenasoldout.com/accept-invite?token=…`,
  в логе только идентификаторы. Проверено на стенде: ambasafe@abhteam.com, задача выполнена через
  Brevo SMTP.
- **F-39 🟠** Сеанс с местами: категории из схемы зала создаются как **бесплатные** («Parter — free»);
  если забыть цену — места продаются даром. В таблице категорий у сеанса «СВОБОДНО —» вместо числа
  мест; в превью схемы видны служебные образцы цветов и легенда; точка цвета Parter белая.
- **F-40 🔴** Афиша в админке Arena не сохраняется видимо: после «Session poster updated» (POST
  /v1/media 201 + PATCH сеанса 200) и «applied to all sessions» (PATCH мероприятия 200) карточка
  после переоткрытия показывает у обоих сеансов «No poster», в списке — заглушка; `GET
  /v1/sessions/{id}/media` 4 раза ответил 400. Разобрать. Связанное: афиша, пришедшая импортом из
  ивент-центра, хранится на мероприятии (`poster_media_id`) и отдаётся сайту, но в админке у сеанса
  «No poster», в списке мероприятий миниатюры нет.
- **F-41 🔴** Гибридный сеанс на смешанной схеме «Palac Akropolis GA» (90 мест балкона + зона 500
  стоячих): создаётся только категория мест («Third», вместимость 90); зона GA из схемы категорией не
  становится.
- **F-42 🔴** В гибридном сеансе правка категории с местами требует «КОЛИЧЕСТВО» (как у категории
  без мест) — «Save changes» неактивна, цену местам задать нельзя, продать их нельзя. В чистом
  «Assigned seats» то же поле необязательное («из схемы зала»). Мероприятие «ASO Seated — Akropolis
  Balcony» оставлено черновиком.
- **F-43 🔴** Цвета категорий из SVG-схемы теряются: на сайте Arena отдаёт `Parter:#eeeeee`,
  `Balcony:#111111` вместо `#e53935`/`#1e88e5` — места партера почти белые на светлом фоне,
  покупатель их почти не видит (в админке та же белая точка у Parter).
- **F-44 🟠** После продажи мест схема на странице разово не загрузилась («Failed to load the seating
  plan») — причина: магазин в этот момент переключили на канал #4, и `GET_SEAT_LIST` для мероприятия
  abhteam ответил `-3 session not found in this channel's organization`. Поведение шлюза верное, но
  товары старой организации остаются опубликованными и сломанными — при настоящем переключении
  сайта их надо снимать.
- **F-45 🟠** Мероприятие с двумя датами синхронизируется двумя товарами с одинаковым названием
  («Nordic Lights — Prague Night» ×2) — дата в названии/карточке не видна. В выбранных местах
  («Parter · Seat 5») не показан ряд.
- ✅ Продажа мест на сайте: схема (66 мест), выбор 2 мест мышью, бронь 19:57, оплата → #12822
  «Completed», 2 PDF на ambasafe@, MACS получил `Parter / ряд 3 / места 5, 6`.
- ✅ Новая организация через канал #4: синхронизация подтянула 3 мероприятия; покупка #12837
  (2× Balcony) — сбор канала 1% применён (90,90 €), подписанный `order.paid` принят сайтом
  (`charge=0.9`), письмо ушло. MACS для org #4 ещё не зарегистрирован → в логе
  `macs.subscriber_missing` (исправление B-1 работает); заказ 1000001651 долить выгрузкой.
- ✅ Схема зала: создание (после 01d692d), загрузка SVG (66 мест, 2 категории, превью), сеанс
  «Assigned seats» с выбором схемы, подсказка «20:00 CET», публикация в канал #4.
- **F-32 🔴 (исправлено 01d692d)** Создание схемы зала супер-админом: сервер писал в один ответ
  два JSON (`superadmin.missing_reason` + `seating_plan.owner_org_forbidden`), админка показывала
  «Server returned a non-JSON response»; админка к тому же не слала `X-Admin-Reason` на
  create/update/version/fork схем. Исправлены оба конца, тесты в `hseating` и `reason.test.ts`.
- **F-15 🟠** Таблица публикаций: область «CITY 01A0B8F0…» — UUID вместо названия города.
- **F-16 🟠** Смешанный язык в формах категорий: «КОЛИЧЕСТВО», «Добавить категорию», «ВИД»,
  «без места» по-русски среди английских полей.
- **F-17 🟠** Карточка билета: устаревшие плашки «BACKEND GAP» (orders/events endpoints уже
  есть), место показано служебным ключом `ga|t2|000003`.
- **F-18 🟠** Страница билетов: счётчики «refunded: 5 · cancelled: 5» считают одни и те же билеты
  дважды; в фильтре статусов несуществующие issued/redeemed/expired.
- ✅ Организация, 2 организатора (роль Organizer, ссылка на пароль на 1 час), 5 площадок в 5 поясах
  (Прага, Берлин, Таллин, Лондон, Нью-Йорк), города Berlin/London/New York созданы прямо из формы
  площадки, канал #4. Подсказка местного времени у сеанса верна: «20:00 CET», «20:00 GMT-5»
  (переход UTC-даты для Нью-Йорка). Бесплатная категория (free) создаётся.
- ✅ Площадка: поиск адреса OSM заполняет адрес, индекс, город, страну, координаты; все V-1
  поля (часовой пояс, телефон, email, сайт, вместимость) сохраняются и читаются обратно (B-2);
  очистка часового пояса блокируется (B-3); сервер отвергает неизвестный пояс.
