# Нагрузочное тестирование arena — шаг 1, локальный стенд

Дата: 2026-09-13. Окружение: локальный docker compose на машине разработчика
(Docker Desktop, 16 ядер, 15 ГБ), образ API собран с master `5fca24d`.
Серверы (стенд, стейджинг, продакшн) не затрагивались.

Целевая нагрузка, согласованная с владельцем: **200 одновременных посетителей
и 50 заказов в минуту** на одно мероприятие в момент старта продаж.

Проверялись оба входа, через которые arena продаёт билеты:

- **Шлюз, совместимый с Bil24** (`/compat/bil24/json`). Через него продают
  переезжающие WordPress-сайты (Lampyris, Vino&Co).
- **Собственное публичное API** (фид → `checkout/start` → оплата → статус).
  Через него работает встраиваемый виджет.

Набор тестов и инструкция по запуску: `ops/loadtest/README.md`, раздел
«Sales-path suite».

## Итог в одном абзаце

Двойных продаж нет ни на одном входе, остатки в базе сходятся после каждого
прогона. Шлюз держит целевую нагрузку: 248 покупок за 5 минут, билеты выпущены
все. Но найдены два дефекта, которые блокируют реальные продажи: брошенные
корзины никогда не возвращаются в продажу, а виджет одной площадки не может
принять больше 100 запросов в минуту на всех покупателей сразу. Ещё три
дефекта серьёзны, но проявляются реже.

## Находки

Порядок — по влиянию на продажи.

### 1. Брошенные брони никогда не освобождаются — КРИТИЧНО

Если покупатель забронировал билеты и ушёл, билеты остаются заблокированными
навсегда. Мероприятие показывает «распродано», хотя ничего не продано.

- **Доказательство.** Сценарий `gateway.js SCENARIO=expiry`: 20 покупателей
  бронируют весь пул и уходят. Срок брони 120 с. Через 240 с новый покупатель
  получает «Category is sold out». Аудит: 20 броней в состоянии `draft`,
  срок истёк, `capacity_held = 20`, продано 0.
- **Причина.** Код освобождения есть (`hcheckout.ReservationProcessor.ProcessExpiredReservations`),
  но его не вызывает ни API, ни `arena-worker`: в `registerBuiltinHandlers`
  такой задачи нет. Периодическая `order.expire_sweep` закрывает только
  заказ и сама в комментарии ссылается на «существующий обработчик TTL
  броней», которого нет.
- **Масштаб.** Механизм броней общий, поэтому страдают оба входа. Скорее всего,
  именно этим объясняется «распродано» у категории Standard на тестовом
  мероприятии стейджинга Lampyris. На сервере это не проверялось.
- **Что сделать.** Зарегистрировать периодическую задачу в `arena-worker`,
  которая вызывает `ProcessExpiredReservations`. Покрыть интеграционным тестом
  «бронь истекла → остаток вернулся».

### 2. Лимит 100 запросов в минуту на весь виджет площадки — КРИТИЧНО для виджета

Публичное API ограничивает частоту запросов по токену фида: 100 в минуту.
Токен один на канал продаж, то есть на всех покупателей виджета сразу.

- **Доказательство.** Полный поток через собственное API (200 посетителей,
  50 заказов в минуту, у каждого посетителя свой IP): 15 884 из 16 543
  запросов получили 429 `feed.rate_limited`, прошли 6 покупок из 250.
  Гонка за 10 билетов: 100 из 200 покупателей получили 429 ещё до попытки.
- **Причина.** `newPublicFeedRateLimiter(100, 300)` в `httpserver/wire.go`,
  значения зашиты в код, настройки нет.
- **Что сделать.** Считать лимит по посетителю, а не по площадке, и сделать
  его настраиваемым. См. также находку 8 про доверие к X-Forwarded-For.

### 3. Покупатель виджета не видит оплаченный заказ — ВЫСОКИЙ, требует проверки на Stripe

После успешной оплаты заказ помечается оплаченным, бронь переходит в
проданные, билеты выпускаются. Но `checkout_sessions.state` остаётся
`pricing_confirmed`. Публичный статус (`GET /v1/public/checkout/{token}`)
поэтому отвечает `pending` и не отдаёт билеты.

- **Доказательство.** Все 6 оплаченных покупок полного прогона и все
  покупки малого прогона: заказ `paid`, билеты есть, чекаут `pricing_confirmed`.
- **Оговорка.** Оплату в тесте эмулирует вебхук платёжного намерения,
  настоящего Stripe локально нет. Нужно проверить, завершает ли боевой путь
  Stripe чекаут иначе.
- **Связанное.** Вебхук принимает `succeeded` только из состояния `processing`.
  Прямой переход `created → succeeded` подтверждается ответом 200 и молча
  игнорируется. Stripe для карт часто присылает сразу `succeeded`. Если боевой
  путь идёт через этот обработчик, оплата будет потеряна без ошибки.

### 4. Взаимная блокировка при бронировании через шлюз — ВЫСОКИЙ

Под параллельной нагрузкой бронь иногда падает с ошибкой базы
`deadlock detected`. Сайт получает код -99, «внутренняя ошибка, не повторять».

- **Доказательство.** 2 случая за 5 минут полного прогона (13,7 тыс. запросов),
  лог `RESERVATION: cart mutation failed ... 40P01`.
- **Причина.** `hcheckout.CreateGAHold` (создание новой корзины) сначала
  блокирует `inventory_ledger`, потом строку `sessions` (`seat_status_version`).
  `ExtendHoldTx`, `ShrinkHoldTx`, `CreateSeatedHold` берут те же строки в
  обратном порядке. Повтора при 40P01 нет. `writeCartHoldError` отдаёт -99
  вместо -1.
- **Что сделать.** В `CreateGAHold` поднять `IncrementSessionSeatStatusVersion`
  перед `ReserveCapacity`. Добавить короткий повтор при 40P01/40001. Отдавать
  -1 на инфраструктурные ошибки. Тест: параллельные создание и сокращение
  корзины на одном GA-сеансе.

### 5. Потолок производительности шлюза — СРЕДНИЙ

Каждая команда шлюза проверяет токен сайта через bcrypt, кэша нет.

- **Доказательство.** Даже простейшая команда отвечает не быстрее 50 мс,
  тогда как чтение фида собственного API — 5–8 мс. При 44 запросах в секунду
  API занимал 3–4 ядра, база — около 30 %. Замер bcrypt cost 10 на этой
  машине: 50–190 мс процессорного времени на вызов.
- **Влияние.** 50 заказов в минуту шлюз держит. В пике одновременных
  бронирований (гонка 200 покупателей) каждое бронирование ждало около 3 с.
  Все брони на одном сеансе к тому же выстраиваются в очередь на строке
  `sessions` (см. находку 4).
- **Что сделать.** Кэшировать успешную проверку токена в памяти
  (например, по SHA-256 токена с коротким сроком жизни).

### 6. Выдача доступа к шлюзу обнуляет срок брони канала — СРЕДНИЙ

`PUT .../channels/{id}/gateway-credential` сбрасывает
`sales_channels.reservation_ttl_override` в NULL. Бронь получает стандартные
20 минут вместо настроенного срока.

- **Доказательство.** Канал создан с 120 с, после PUT в базе NULL
  (воспроизведено вручную).
- **Причина.** В `UpdateSalesChannel` (`queries/channels.sql`)
  `reservation_ttl_override = $8` присваивается всегда, в отличие от
  остальных полей. Любой частичный вызов обновления канала стирает настройку.

### 7. Второй заказ того же покупателя закрывает первый — СРЕДНИЙ

arena держит один неоплаченный заказ на покупателя в сеансе, покупатель
определяется по email и телефону. `CREATE_ORDER_EXT` с тем же email истекает
предыдущий заказ, даже если у него своя действующая бронь. Оплата первого
заказа (`PAY_ORDER`) затем падает `invalid status transition: expired → paid`
и отдаётся сайту как временная ошибка.

- **Когда бывает.** Покупатель оформляет два заказа в двух вкладках. Деньги за
  первый уже списаны WooCommerce, а билетов нет и ручная проверка не
  запускается.
- **Доказательство.** Воспроизвелось в первом прогоне, где у тестовых
  покупателей был общий email. В тестах это исправлено.

### 8. Ограничитель частоты доверяет X-Forwarded-For — НИЗКИЙ, безопасность

`httputil.ExtractClientIP` берёт первый адрес из `X-Forwarded-For`, который
присылает сам клиент. Лимит по IP обходится подменой заголовка. Словари
счётчиков в ограничителе не очищаются, поэтому поток подменённых адресов
растит память.

### 9. Мелочи

- Старые сценарии `scanner.js` и `checkout.js` не могли авторизоваться:
  `shared/auth.js` слал поля, которые сервер отвергает. **Исправлено.**
- Workflow `.github/workflows/load-test.yml` ни разу не запускался.
- OpenAPI описывает создание канала полями `{name, type}`, сервер требует
  `payment_mode`, `provider`, `provider_account_id`. **Исправлено в волне 3.**

## Что работает

| Проверка | Результат |
|---|---|
| Шлюз, полный поток, 5 мин | 248 покупок, все билеты выпущены, ошибок 0,02 % |
| Шлюз, время ответа p95 | каталог 108 мс, бронь 127 мс, заказ 108 мс, оплата 120 мс |
| Шлюз, путь покупателя p95 | 531 мс от создания пользователя до оплаты |
| Шлюз, выпуск билетов p95 | 101 мс после оплаты |
| Гонка через шлюз, 200 на 10 билетов | продано 10, отказ «распродано» 190, ошибок 0 |
| Гонка через собственное API | продано 10, отказ 90, ещё 100 отсечены лимитом (находка 2) |
| Собственное API, время ответа p95 | список 65 мс, карточка 56 мс, старт чекаута 59 мс под нагрузкой |
| Аудит остатков после каждого прогона | двойных продаж 0, расхождений остатков 0 |

## Ограничения шага 1

- Цифры скорости сняты на Docker Desktop под Windows. Они показывают порядок
  величин, а не боевые значения. Честные цифры даст шаг 2 на отдельной копии
  стенда.
- Настоящий Stripe не участвовал, оплата виджета эмулирована.
- Вебхуки на сайты и MACS не нагружались: воркер локально работает в режиме
  без отправки. Это отдельный сценарий.
- Пропускную способность собственного API без лимита измерить нельзя, пока
  лимит зашит в код.

## Длительный прогон

Шлюз, 60 минут, половина целевой нагрузки: 100 посетителей, 25 заказов и
10 брошенных корзин в минуту. Брошенные корзины здесь явно снимают бронь
(`UN_RESERVE_ALL`), поэтому находка 1 в этом прогоне не проявляется.

| Показатель | Результат |
|---|---|
| Запросов | 82 424, ошибок 0 |
| Покупок | 1 501, неудачных 0, все билеты выпущены |
| Время ответа p95 | каталог 117 мс, бронь 159 мс, заказ 113 мс, оплата 126 мс |
| Путь покупателя p95 | 576 мс |
| Память API | 36–48 МБ на всём прогоне, роста нет |
| Соединений с базой | 20–30, роста нет |
| Очередь outbox | 0–3 события, не копится |
| Аудит остатков | продано 2 987 = билетов 2 987, отменено 601 бронь, нарушений 0 |

Взаимных блокировок на половинной нагрузке не было. Деградации со временем
не видно: утечек памяти, соединений и очередей нет.

## Волна исправлений 1 — находки 1 и 2

Исправлено 2026-09-13, проверено повтором тех же сценариев на локальном стенде.

**Находка 1 — брошенные брони.** Добавлена периодическая задача воркера
`reservation.expire_sweep` (каждые 30 с, до 500 броней за проход). Обработчик
просроченных броней и снятие брони теперь освобождают остатки одной общей
функцией, поэтому не разойдутся снова. При первом запуске воркер освободил
44 брони, зависшие после прежних прогонов.

**Находка 2 — лимиты виджета.** Лимиты стали раздельными и настраиваемыми:
на площадку (токен фида, по умолчанию 20 000 в минуту), на чекаут покупателя
(120 в минуту) и на IP (600 в минуту). Ответ 429 несёт `Retry-After`,
счётчики очищаются. По дороге исправлено определение IP за прокси:
- промежуточный слой роутера брал адрес из подменяемого заголовка;
- функция доверенного IP смотрела на одну позицию левее нужной, и за
  одним прокси все посетители получили бы адрес самого прокси;
- лимит сканера и журнал запросов доверяли заголовку даже без прокси.

**Важно для развёртывания.** За Traefik в Dokploy нужно выставить
`TRUSTED_PROXY_COUNT=1`, иначе все посетители будут считаться одним IP.

| Сценарий | До | После |
|---|---|---|
| Брошенные корзины, срок брони 120 с | через 240 с «распродано», 20 из 20 заблокированы | весь пул снова доступен |
| Гонка через собственное API, 200 на 10 | 10 продано, 100 отсечены лимитом | 10 продано, 190 «недостаточно мест», 0 отсечено |
| Полный поток через собственное API | 6 покупок из 250, 96 % запросов 429 | 251 из 251, 0 отказов, ошибок 0 |
| Старт чекаута p95 | 59–635 мс | 25 мс |
| Путь покупателя p95 | 387 мс | 47 мс |
| Загрузка процессора API | — | около 10 % |

Гейты: юнит-тесты, линтер, gofmt и все интеграционные тесты на свежей базе
зелёные. Находка 8 закрыта для ограничителей частоты. Для полей журнала
аудита `ExtractClientIP` по-прежнему читает заголовок напрямую. Открытыми
остаются находки 3–7.

## Волна исправлений 2 — находки 3 и 4

Исправлено 2026-09-13, проверено повтором сценариев на локальном стенде.

**Находка 4 — взаимная блокировка.** Создание новой корзины теперь берёт
блокировки в общем порядке: строка сеанса, затем остатки, затем единицы
билетов. Та же инверсия найдена и исправлена в `POST /v1/reservations`.
Транзакции броней повторяются до трёх раз при конфликте. Шлюз отвечает
сайту «временная ошибка» только на ошибки базы, которые лечатся повтором.
Интеграционный тест при старом порядке ловил 26 блокировок за 3 секунды,
после исправления — ни одной.

**Находка 3 — оплаченный чекаут.** Вебхук оплаты завершает чекаут в той же
транзакции, поэтому покупатель видит «оплачено» и билеты. Вебхук принимает
настоящий конверт события Stripe и событие успеха сразу из состояния
«создан», как Stripe присылает для карт. Оплата, пришедшая после истечения
чекаута, не завершает его принудительно. Заказ уходит на ручную проверку,
а успешный платёж сохраняется. Публичный статус дополнительно показывает
«оплачено», если заказ оплачен.

| Сценарий | До | После |
|---|---|---|
| Шлюз, полный поток, 5 мин | 248 покупок, 3 сбоя, 2 блокировки | 251 покупка, 0 сбоев, 0 блокировок, 0 ошибок в журнале |
| Шлюз, гонка 200 на 10 | чисто | чисто, 0 блокировок, бронь p95 1,7 с вместо 3,2 с |
| Виджет, полный поток | 251 оплата, 0 показаны покупателю | 251 оплата, 251 показаны, чекауты `completed` |
| Виджет, выпуск билетов виден покупателю | никогда | около 1 с |

**Важное ограничение, выяснившееся при разборе.** Виджет пока вообще не
принимает оплату. После старта чекаута он уводит покупателя на
`/checkout/<id>`, за которым нет ни страницы, ни обработчика. Платёж у
провайдера никто не создаёт: адаптеры Stripe и AllPay написаны, но нигде не
вызываются, а создание платёжного намерения требует токен организации.
Исправление 3 делает серверную часть готовой к оплате. Сам приём денег через
виджет — отдельная фича, для неё нужны решения владельца.

Гейты: юнит-тесты, линтер, gofmt и все интеграционные тесты на свежей базе
зелёные. Открытыми остаются находки 5–7 и фича оплаты виджета.

## Волна исправлений 3 — находки 5, 6 и 7

Исправлено 2026-09-13, проверено повтором сценариев на локальном стенде.

**Находка 5 — потолок шлюза.** Успешная проверка токена сайта кэшируется в
памяти на 5 минут (`BIL24_TOKEN_CACHE_TTL`). Кэш привязан к хэшу токена,
поэтому смена токена действует сразу. Выключенный канал отсекается до
обращения к кэшу. Неверный токен всегда проверяется bcrypt заново.
Одновременные первые запросы с одним токеном ждут одну проверку, а не
запускают по одной на запрос.

Первая версия кэша не дала эффекта, и нагрузка это показала: сервер создаёт
обработчик шлюза заново на каждый запрос, а кэш жил внутри обработчика и был
пустым каждый раз. Юнит-тесты этого не видели, потому что переиспользовали
один обработчик. Кэш перенесён на сервер, добавлены тесты на эту разводку.

| Шлюз, полный поток, 5 мин, 44 запроса/с | До | После |
|---|---|---|
| API, процессор в среднем | 346 % | 25 % |
| Полная покупка, медиана / p95 | 422 / 553 мс | 66 / 160 мс |
| Бронирование, медиана | 88 мс | 15 мс |
| Каталог (`GET_ALL_ACTIONS`), медиана | 93 мс | 21 мс |
| Гонка 200 на 10, бронирование p95 / максимум | 1,7 с / 3,2 с | 350 / 365 мс |

Ошибок 0, 251 покупка, все билеты выданы. Гонка: 10 продано, 190 получили
«распродано».

**Находка 6 — срок брони канала.** `UpdateSalesChannel` меняет срок брони
только по явному флагу. Выдача и отзыв доступа к шлюзу срок не трогают.
В `PATCH` канала: поля нет — срок сохраняется, `null` — сбрасывается к
стандартному, число — задаётся, 0 и отрицательные значения отклоняются.
Ошибка задевала и админку: правка одного названия канала стирала срок.
Форма админки уже шлёт поле только при изменении, поэтому после исправления
работает без правок. Описание создания и изменения канала в OpenAPI
исправлено под реальные поля (пункт из «Мелочей»). `provision.mjs` больше
не обходит ошибку, а проверяет, что срок сохранился после выдачи доступа.

**Находка 7 — второй заказ того же покупателя.**
- `CREATE_ORDER_EXT` закрывает прежний заказ покупателя только если его бронь
  действительно истекла. Пока бронь жива, второй заказ получает понятный
  отказ `101` («у вас уже есть неоплаченный заказ на это событие») и ничего
  не записывает. Повтор `CREATE_ORDER_EXT` из той же корзины по-прежнему
  обновляет заказ.
- `PAY_ORDER` на закрытый по сроку заказ больше не отвечает «временная
  ошибка» бесконечно. Если места удаётся удержать или взять заново, заказ
  оживает, оплачивается и получает билеты, в истории заказа остаётся событие
  `revived_for_payment`. Если мест нет, заказ уходит на ручную проверку с
  сигналом оператору и ответом `101`.
- Ревью нашло ещё один бесконечный цикл: у покупателя уже есть новый
  неоплаченный заказ на тот же сеанс, а оплата приходит за старый. Оживление
  упиралось в правило «один неоплаченный заказ», теперь такой заказ уходит на
  ручную проверку с причиной `superseded_by_open_order`.
- Повтор `PAY_ORDER` на заказ, уже стоящий на ручной проверке, отвечает `101`
  без повторных записей и без второго сигнала оператору.

Новый режим нагрузки: `SHARED_BUYERS=3 PAY_DELAY_SECONDS=10`, три общих email
и телефона, между заказом и оплатой пауза до 10 с. За 3 минуты 82 покупки,
69 вторых заказов отклонены кодом `101`, неудачных оплат 0, ошибок
`expired → paid` 0. Порог k6 на время покупки в этом режиме превышается по
построению из-за паузы. Сверка базы после всех прогонов: двойных продаж,
расхождений остатков и зависших броней нет, все 583 заказа сеанса оплачены.

### 10. Одновременное создание нового покупателя — НИЗКИЙ, найдено в волне 3

Два одновременных `CREATE_USER` с одним новым email упираются в уникальный
индекс `customer_identities_strong_uq`. Шлюз отвечает `-1`, повтор находит
созданного покупателя, так что для сайта это одна лишняя попытка (1 случай за
прогон с общими покупателями). Тот же поиск покупателя в чекауте виджета
(`createPublicOrder` в `hfeed/public_feed_checkout.go`) идёт на транзакции
подтверждения чекаута без SAVEPOINT и глотает ошибку как «некритичную». По
коду, после такой ошибки базы транзакция уже прервана, и подтверждение
чекаута отвечает 500 (правило SAVEPOINT из AGENTS.md). Под нагрузкой не
воспроизведено: в сценарии виджета у покупателей разные email. Исправление: при нарушении
уникальности перечитывать покупателя, а в чекауте виджета — выполнять поиск
в SAVEPOINT.

**Исправлено.** `customers.Resolve` теперь сам защищён от гонки: вставка
покупателя и первой строки `customer_identities` идёт внутри SAVEPOINT
(`gen.Queries.DB()` + `Begin` — настоящий SAVEPOINT поверх чужой транзакции
или отдельная транзакция поверх пула), при нарушении уникального индекса
резолвер откатывает эту вставку и повторяет поиск заново — проигравший
получает того же покупателя, а не ошибку 23505, и лишней строки-сироты в
`customers` не остаётся. `LinkOrg` (`customer_org_links`) уже был
`ON CONFLICT DO NOTHING` и правки не потребовал. В `createPublicOrder`
(`hfeed/public_feed_checkout.go`) резолв покупателя и привязка к организации
теперь тоже завёрнуты в отдельный SAVEPOINT (`Handler.bestEffort`, по образцу
`hbil24.Handler.payBestEffort`), так что даже редкая гонка мимо внутренней
защиты резолвера больше не прерывает транзакцию подтверждения чекаута.
Добавлены юнит-тесты на имитацию гонки (fakeStore с инъекцией конфликта) и
интеграционные тесты на живой базе: параллельные вызовы `customers.Resolve`
с одним новым email, параллельные `CREATE_USER` через шлюз-харнесс и два
параллельных `POST /checkout/start` виджета с одним новым покупателем — все
зелёные, без 500 и без лишних строк в `customers`.

Гейты: юнит-тесты, линтер, gofmt, интеграционные тесты на свежей базе
зелёные. Открытой остаётся фича оплаты виджета; находка 10 закрыта.

## Окно оплаты 20 минут, без ручных проверок

Решение владельца 2026-09-13: покупателю даётся фиксированное окно на оплату
от момента создания заказа (`CREATE_ORDER_EXT`), по умолчанию 20 минут плюс
2 минуты запаса на задержку сети сайта. После истечения окна оплата не
принимается — заказ автоматически истекает или отменяется, место или GA-юнит
сразу возвращается в продажу. Ручной проверки в шлюзе больше нет вообще: у
владельца нет персонала, чтобы её разбирать, поэтому каждый исход `PAY_ORDER`
теперь решается автоматически.

Это отменяет находки выше про «оживление» просроченного заказа
(`ReviveForPayment`) и про парковку в `manual_review` при недоступной брони
(`superseded_by_open_order`, повтор `PAY_ORDER` без повторных записей) — оба
механизма были нужны только потому, что заказ мог зависнуть в неопределённом
состоянии без чёткого дедлайна. Фиксированное окно убирает саму причину:
просроченный заказ истекает сразу и без возврата к оплате, а бронь, отнятую
конкурентом ВНУТРИ окна, заказ отменяет сам, тоже без участия оператора.
`PAY_ORDER` теперь берёт блокировку строки заказа первым же действием
транзакции, поэтому параллельные попытки оплаты одного заказа (в том числе на
границе дедлайна) сериализуются, а не гоняют гонку чтение-затем-запись.

Сторона сайта (Lampyris, ветка `feat/payment-window`): деньги в Stripe сначала
замораживаются и списываются только после ответа `PAY_ORDER` = 0. При отказе
заморозка снимается. В момент срока платёж Stripe отменяется, а заказ
WooCommerce аннулируется. Сорванное списание аннулирует билеты. Неудачный
автовозврат повторяется до 7 дней. На staging нужно выключить в настройках
Stripe «Capture charge immediately».

### Повторная нагрузка после окна оплаты и находки 10

Локальный стенд, образ собран с master после объединения.

| Сценарий | Результат |
|---|---|
| Шлюз, полный поток, 5 мин | 251 покупка, 0 ошибок, API в среднем 23 % процессора, медиана покупки 66 мс |
| Шлюз, гонка 200 на 10 | 10 продано, 190 «распродано», бронь p95 292 мс |
| Шлюз, окно оплаты (60 с + 10 с, `SCENARIO=paywindow`) | срок оплаты верен у 20 из 20 заказов; 10 оплат вовремя приняты, 10 после срока отклонены; ровно 10 мест вернулись в продажу, одиннадцатое занять нельзя |
| Шлюз, общие покупатели | 85 покупок, 66 вторых заказов отклонены, 0 неудачных оплат, 0 ошибок создания покупателя |
| Шлюз, брошенные корзины | весь пул снова доступен после срока брони |
| Виджет, полный поток | 250 оплат, все показаны покупателю, 0 ограничений частоты |
| Виджет, гонка 200 на 10 | 10 продано, 190 чистых отказов |

Сверка базы по сеансам потока, гонки, окна оплаты и брошенных корзин: нарушений
нет. Заказов в `manual_review` не создано. Ошибок в журналах API и воркера нет.
