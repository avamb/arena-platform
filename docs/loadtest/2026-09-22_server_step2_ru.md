# Нагрузочный прогон на сервере — шаг 2 (22.09.2026)

Цель, поставленная владельцем: определить, какая инфраструктура нужна
для минимальной, средней и высокой нагрузки — ступенчато поднимать
нагрузку до предела, назвать, во что упирается стенд, и дать матрицу
серверов. Прод (`arena-platform-prod-1`, 178.105.217.195) не трогался.

## 0. Итог простыми словами

**Что мы делали.** Подняли точную копию рабочего сервера на отдельной
машине, посадили рядом «толпу» из тысячи покупателей, которая покупала
билеты всё быстрее и быстрее, и смотрели, в какой момент и от чего
система начинает захлёбываться. Рабочий сервер не трогали.

**Главный вывод: сервер, который сейчас стоит на проде, справится с
большим запасом.** Он спокойно продаёт 800 заказов в минуту в одно
событие, когда на сайте одновременно сидит тысяча человек, и при этом
занят меньше чем наполовину. Для сравнения, «высокая нагрузка» по нашей
шкале — это 200 заказов в минуту при той же тысяче посетителей, то есть
запас в четыре раза. Более дешёвый сервер вдвое меньше (2 ядра вместо 4)
тоже держит минимальную и среднюю нагрузку, а высокую — только после
правки настройки ниже.

**Что ломалось первым — и это была не мощность сервера, а одна
настройка и один участок кода.** Когда одновременно много людей
смотрят события и несколько человек в секунду нажимают «купить», каждая
покупка на короткое время «запирает» строку события в базе. В это время
код ходил в базу за ценами и за проверкой платёжных настроек через
общую очередь соединений (их было 20), а очередь уже была занята теми,
кто ждёт, пока событие «отопрут». Получалась пробка, в которой все ждут
всех, процессор при этом простаивает, и через 30 секунд у покупателей
вылетали ошибки. Простое увеличение очереди соединений с 20 до 60 на
той же слабой машине превратило «11 успешных покупок из 601» в «601 из
601». Мы сделали и это, и правильное лечение: код больше не ходит в
общую очередь, пока держит событие запертым.

**Что ещё выяснилось.**
- На событиях с очень большим количеством мест в одной категории
  (десятки тысяч, стадионный вариант) операции с местами были в разы
  медленнее из-за отсутствия индекса. Индекс добавлен: выделение мест
  на сеансе в 66 000 мест ускорилось с 150 мс до 0,2 мс.
- Через Cloudflare всё работает, добавляет примерно 0,1 с к каждой
  покупке. Но прод-прокси не сообщал системе настоящий адрес
  посетителя, из-за чего защита от перегрузки по адресу считала всех
  посетителей одним человеком — это надо включить на проде (§6).
- Как считать нагрузку, важнее, чем какой сервер: если считать, что
  каждый посетитель WordPress-сайта сам дёргает наш каталог, шлюз
  «ломается» на 50 заказах в минуту, а если считать как сайты работают
  на самом деле (каталог тянет плагин, с кэшем), тот же сервер держит
  400 и больше.

**Что сделано по итогам** (подробно в §6): правка кода чекаута и индекс
(в репозитории, ждут выкатки), увеличение очереди соединений и
доверие адресам Cloudflare — настройки прода, для них нужен один
деплой и правка конфига Traefik.

**Про более дешёвый сервер** — §8: переезжать сейчас не стоит, экономия
около 16 евро в месяц не окупает переезд с риском; но если переезд
случится по другой причине, есть варианты вдвое дешевле.

## 1. Что было поднято

| Роль | Сервер | Тип | Где |
|------|--------|-----|-----|
| копия стенда | `arena-loadtest-1`, 128.140.12.240 | cpx22 → cpx32 → cpx42 (Rescale) | Hetzner nbg1 |
| генератор | `arena-loadgen-1`, 49.13.205.229 | cpx22 | Hetzner nbg1 |

- Прод на самом деле **cpx32** (AMD, 4 vCPU, 8 ГБ, 160 ГБ), а не CX33, как
  считалось по `lscpu`. Линейка CX22/CX23/CX33/CX43 22.09 не размещалась ни
  в fsn1, ни в nbg1, ни в hel1 («error during placement»), cpx22 в fsn1 —
  тоже; поэтому лестница идёт по линейке прода в nbg1.
- Стек копии: `ops/loadtest/server/docker-compose.copy.yml` (тот же образ
  `ghcr.io/avamb/arena-api:d84a4de`, что на проде, те же `mem_limit`:
  db 1 ГБ, api и worker по 512 МБ) плюс новый
  `docker-compose.traefik.yml` — свой Traefik v3.5 с Let's Encrypt вместо
  подключения к боевой панели Dokploy. Свежая база, миграции до 0107,
  `arena-seed` через SSH-туннель. Stripe — заглушка `stripe-stub.cjs`,
  почта в лог, Telegram-сторож выключен.
- Файрвол Hetzner `arena-loadtest-lab`, три DNS-записи
  `loadtest-{api,worker,tickets}.arenasoldout.com` с серым облаком.
- Генератор: k6 0.54 в Docker, `capacity.mjs` (лестница), на копии
  `monitor.mjs` (docker stats + снимок Postgres каждые 15 с).

Подробности и грабли — `docs/loadtest/server_run_runbook_ru.md` §1.

## 2. Метод

Лестница `capacity.mjs`: одна ступень = 3 минуты k6 на фиксированной
интенсивности заказов (`constant-arrival-rate`) плюс толпа читающих
посетителей (`constant-vus`, 5 посетителей на заказ в минуту, потолок
1000 — столько держит генератор на 2 vCPU). Ступени 10, 25, 50, 100, 200,
400 заказов/мин, пауза 45 с. Ступень «сломана», если: доля ошибок > 0,5 %,
p95 полного пути покупки > 1 с, есть неудачные покупки, оплаченные
билеты не показаны покупателю, или k6 не удержал темп (dropped
iterations — предел генератора, а не стенда). Отдельно считаются 5xx
(стенд) и транспортные ошибки (путь до стенда).

Полный путь покупки: виджет — список событий → событие → `checkout/start`
(hosted-сессия на заглушке) → подписанный вебхук
`checkout.session.completed` → опрос статуса до появления билетов; шлюз —
CREATE_USER → GET_ALL_ACTIONS → RESERVATION → GET_CART →
CREATE_ORDER_EXT → PAY_ORDER → GET_TICKETS_BY_ORDER.

Уровни нагрузки (черновик, ждёт подтверждения владельца):

| Уровень | Посетителей одновременно | Заказов/мин |
|---------|--------------------------|-------------|
| минимальный | 50 | 10 |
| средний | 200 | 50 |
| высокий | 1000 | 200 |

Пул мест на лестницу — 6 000 (большой зал; заказ берёт 1–3 билета);
причина именно такого размера — в §3.1.

## 3. Находки до лестницы

### 3.1. Стоимость GA-команд растёт с размером категории

Один и тот же cpx22, пустой стенд, 1 посетитель, 6 заказов/мин, медианы:

| Команда шлюза | пул 60 000 мест | пул 2 000 мест |
|---------------|-----------------|----------------|
| RESERVATION | 443 мс | 56 мс |
| GET_CART | 199 мс | 19 мс |
| CREATE_ORDER_EXT | 205 мс | 21 мс |
| GET_ALL_ACTIONS | 48 мс | 49 мс |
| путь покупки | 970 мс | 147 мс |

Команды, которые держат или считают места категории, читают все места
категории, а не только свободные — 30-кратный пул даёт 8–10-кратную
задержку. Для реальных залов (сотни — несколько тысяч мест) это
десятки миллисекунд; для стадионной GA-категории на 20–50 тысяч мест —
сотни. GET_ALL_ACTIONS от размера пула не зависит. Отдельная задача на
индекс/запрос (`session_seats` по `(session_id, tier_id, status)`), не
блокер запуска.

### 3.2. За настоящим прокси посетители сливаются в один IP

Traefik по умолчанию затирает клиентский `X-Forwarded-For`, генератор —
один адрес, и `PUBLIC_API_IP_RATE_LIMIT` (600/мин) резал виджет уже на 20
заказах/мин: 98 × 429, 2 неудачные покупки. На копии генератор внесён в
`forwardedHeaders.trustedIPs` Traefik и `TRUSTED_PROXY_COUNT=2`. Для прода
это напоминание: за Cloudflare без `trustedIPs` для его диапазонов api
видит только адрес edge-узла (в `~/.ssh`-заметке 2026-09-14 это уже
записано), и per-IP лимит превращается в общий.

### 3.3. Мелочи стенда

- Docker 29 отказывает API-клиентам ниже 1.40, Traefik просит 1.24 →
  `"min-api-version": "1.24"` в `daemon.json`.
- Образ `grafana/k6` не root → `results/` на генераторе с правом записи.

## 4. Лестницы

### 4.1. cpx22 (2 vCPU AMD, 4 ГБ) — виджет

Пул 6 600 мест, fixtures `202609221801-8f24`, 18:01–18:24 UTC.

| заказов/мин | посетителей | ok | неудачных | ошибок | 5xx | транспорт | путь p95 | путь p99 | checkout p95 | выдача p95 | req/s | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 10 | 50 | 31 | 0 | 0 % | 0 | 0 | 125 мс | 285 мс | 88 мс | 1,02 с | 13,7 | ok |
| 25 | 125 | 76 | 0 | 0 % | 0 | 0 | 110 мс | 353 мс | 62 мс | 1,02 с | 33,8 | ok |
| 50 | 250 | 151 | 0 | 0 % | 0 | 0 | 87 мс | 92 мс | 59 мс | 1,02 с | 66,9 | ok |
| 100 | 500 | 301 | 0 | 0 % | 0 | 0 | 82 мс | 561 мс | 52 мс | 1,02 с | 133 | ok |
| 200 | 1000 | 600 | 0 | 0 % | 0 | 0 | 172 мс | 3,4 с | 136 мс | 1,07 с | 262 | ok |
| 400 | 1000 | 19 | 1182 | 55,7 % | 0 | 5168 | 33 с | 52 с | 30 с | 26 с | 44 | СЛОМАНО |

Ресурсы копии на ступени 200: api 24 %, postgres 54 %, Traefik 13–19 %
(в долях одного ядра), load 2,7 на 2 vCPU, память api 39 МБ / postgres
280 МБ, соединений 23, ожиданий блокировок 0. Генератор: k6 20 % CPU,
600 МБ на 1000 VU — не предел.

**Что сломалось на 400.** `capacity.mjs` по формальному признаку записал
«транспорт, не arena» (0 × 5xx на стороне k6, статус 0 = таймаут 30 с), но
лог api говорит обратное: за ступень api завершил 9 284 запроса, из них
8 203 дольше 5 с, максимум 30 с; 243 × 500 и 931 × 503 — k6 их уже не
дождался. Монитор: 20 из 20 соединений пула api заняты, 19 ждут
блокировку, самая длинная транзакция 13–28 с, CPU postgres при этом
22–55 %, api 5–27 % — процессор свободен. Postgres логирует отменённый
`UPDATE sessions SET seat_status_version = seat_status_version + 1`: это
та самая блокировка строки сеанса, которую по правилу порядка
блокировок берёт каждая транзакция удержания мест
(`hcheckout.createGAHoldTx`). Все заказы лестницы идут в ОДИН сеанс, и
при ~7 чекаутах в секунду очередь на его строке становится длиннее, чем
пул соединений api (20): следующие запросы — включая список событий и
вебхуки оплаты — ждут свободного соединения, пока клиент не отвалится
по таймауту (`context canceled` во всех 4 000 + 4 000 ошибках списка
тарифов и медиа, 932 отказа «payment config: failed to load the
organization» — это тоже ожидание соединения, а не платёжный конфиг).

Вывод по механике: предел виджета на этом сервере — **между 200 и 400
заказами в минуту в один сеанс**, и упирается он не в CPU, а в
последовательность транзакций удержания на строке сеанса плюс размер
пула соединений. Хвост p99 3,4 с на ступени 200 — начало той же очереди.
Сдвигается ли граница с сервером — покажут cpx32 и cpx42 (§4.3–§4.6);
если нет, это предел «на сеанс», и продажи нескольких событий
складываются, а один ажиотажный сеанс — нет.


### 4.2. cpx22 — шлюз, «каждый посетитель читает каталог»

Пул 6 600, fixtures `202609221826-c024`, 5 читателей каталога на заказ/мин
(GET_ALL_ACTIONS каждые 2–8 с).

| заказов/мин | читателей | ok | неудачных | ошибок | 5xx | путь p95 | путь p99 | CREATE_ORDER_EXT p95 | req/s | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|
| 10 | 50 | 31 | 0 | 0 % | 0 | 282 мс | 1,1 с | 60 мс | 11,4 | ok |
| 25 | 125 | 76 | 0 | 0 % | 0 | 462 мс | 1,1 с | 92 мс | 26,3 | ok |
| 50 | 250 | 150 | 0 | 0,02 % | 2 | 1,9 с | 3,6 с | 293 мс | 51,8 | СЛОМАНО |

На ступени 50 postgres 150–157 % CPU (полтора ядра из двух), api 30 %,
блокировок нет, до 18 активных запросов. Команды (медиана / p95):
RESERVATION 154 / 803 мс, GET_ALL_ACTIONS 98 / 657 мс, CREATE_ORDER_EXT
104 / 293, GET_CART 70 / 264. Два 5xx в логе api не нашлись — это ответы
Traefik, когда api не успел.

Здесь упор — **CPU базы**, и его создают не покупатели, а читатели
каталога: 250 читателей × один GET_ALL_ACTIONS (~50 мс CPU базы даже на
пустом стенде) каждые ~5 с = ~2,5 ядра. Это нереалистичная модель для
шлюза: WordPress-сайты зовут GET_ALL_ACTIONS с сервера, по расписанию и с
кэшем в метаданных товаров, а не по разу на каждого посетителя. Поэтому
§4.3 — та же лестница в режиме «как сайты пользуются шлюзом»: покупатели
по ступеням плюс 10 читателей каталога.

Проверка размера пула под нагрузкой (тот же сервер, пул 1 100, 50
заказов/мин, 250 читателей, 2 мин): путь p95 1 005 мс (медиана 183 против
410 на пуле 6 600), RESERVATION p95 297 против 803, GET_ALL_ACTIONS p95
463 против 657 — пул отвечает за команды удержания, читатели — за
остальное.

### 4.3. cpx22 — шлюз, «как сайты пользуются шлюзом»

Пул 6 600, fixtures `202609221842-…`, 10 читателей каталога на всех
ступенях, покупатели по ступеням.

| заказов/мин | ok | sold_out | ошибок | 5xx | путь p95 | путь p99 | CREATE_ORDER_EXT p95 | выдача p95 | req/s | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|
| 10 | 31 | 0 | 0 % | 0 | 230 мс | 409 мс | 45 мс | 13 мс | 3,9 | ok |
| 25 | 76 | 0 | 0 % | 0 | 279 мс | 458 мс | 52 мс | 15 мс | 5,4 | ok |
| 50 | 151 | 0 | 0 % | 0 | 247 мс | 281 мс | 50 мс | 13 мс | 7,6 | ok |
| 100 | 300 | 0 | 0 % | 0 | 259 мс | 507 мс | 48 мс | 12 мс | 12,3 | ok |
| 200 | 601 | 0 | 0 % | 0 | 226 мс | 249 мс | 47 мс | 9 мс | 22,0 | ok |
| 400 | 1134 | 67 | 0 % | 0 | 228 мс | 295 мс | 54 мс | 15 мс | 39,5 | ok |

Все ступени чистые; 67 sold_out на последней — кончился пул, не дефект.
Ресурсы: на 200 заказах/мин api 40–52 %, postgres 32–44 %; на 400 — api
68–78 %, postgres 55–59 %, ожиданий блокировок 0, активных запросов 1–2.
Путь покупки не растёт с нагрузкой (p95 226–279 мс на всех ступенях):
до 400 заказов/мин в один сеанс шлюз на 2 vCPU не упирается ни во что,
следующий предел — CPU api примерно на 500–550 заказах/мин (экстраполяция
по 78 % на 400).

Сравнение с §4.2 — главный урок про модель нагрузки: одна и та же
машина даёт «сломано на 50» и «чисто на 400» в зависимости от того,
считаем ли мы читателей каталога по посетителям или по сайтам.


### 4.4. cpx22 — виджет: разбор провала на 400 и рычаг пула соединений

Отдельные трёхминутные ступени на том же cpx22, свежие пулы:

| Опыт | заказов/мин | посетителей | пул api | ok | неудачных | путь p95 | api / postgres CPU | итог |
|---|---|---|---|---|---|---|---|---|
| один сеанс, толпа | 400 | 1000 | 20 | 19 | 1182 | 33 с | 5–27 % / 22–55 % | конвой (§4.1) |
| один сеанс, без толпы | 400 | 10 | 20 | 963 | 0 | 78 мс | 9–15 % / 22–34 % | чисто |
| один сеанс, толпа | 300 | 1000 | 20 | 16 | 734 | 31 с | 4–15 % / 20–37 % | конвой |
| два сеанса, толпа | 2 × 200 | 2 × 500 | 20 | 2 × 11 | 2 × 590 | 30 с | — | конвой в обоих |
| два сеанса, толпа | 2 × 200 | 2 × 500 | **60** | 2 × 601 | 0 | 1,0–1,3 с (p99 7–8 с) | — | все покупки прошли, хвост от CPU |

Снимок `pg_stat_activity` в разгар конвоя (300 заказов/мин, 1000
посетителей): 18 бэкендов ждут `IncrementSessionSeatStatusVersion`
(`Lock:tuple`), цепочка блокирующих PID — это очередь на строку сеанса;
держатель блокировки — `idle in transaction` после
`InsertReservationGAItem`, `ClientRead`: он ждёт не базу, а код api. При
10 посетителях те же 400 заказов/мин проходят с путём 78 мс, значит,
удлиняет транзакцию удержания именно толпа читателей: их запросы
занимают пул соединений api (20), транзакция удержания между
операторами ждёт свободное соединение или планировщик, держа при этом
строку сеанса, а следующие удержания встают за ней и занимают
оставшиеся соединения. Пул заполняется ожиданиями, и всё — список
событий, вебхуки оплаты, `/healthz` базы — стоит до таймаута клиента.
Два сеанса по 200 при пуле 20 утонули вместе: пул один на всех, конвой
одного сеанса не изолирован от остальных.

С `DB_POOL_MAX_CONNS=60` (Postgres `max_connections` 100 хватает) та же
пара сеансов — 1 202 покупки из 1 202, без ошибок; остаётся только хвост
задержек, и это уже честный предел двух ядер. Для прода это самая дешёвая
правка из всех найденных: поднять пул api (и посмотреть на
`max_connections` базы). Настоящее лечение — укоротить транзакцию
удержания (всё, что не требует блокировки строки сеанса — резолв
покупателя, прайсинг, конфиг платежа — делать до неё) и не брать
второе соединение из пула внутри открытой транзакции.

### 4.5. cpx32 (тип прода: 4 vCPU AMD, 8 ГБ) — виджет

Rescale cpx22 → cpx32 упёрся в лимит общих ядер проекта Hetzner (прод +
копия + генератор); генератор переведён на `ccx13` (2 выделенных ядра),
после чего копия стала ровно типом прода.

Проверка прод-настройки (пул api 20) на прод-типе: 300 заказов/мин, 1000
посетителей — **898/898, путь p95 73 мс, p99 1,6 с**, api 40 %, postgres
~100 % (одно ядро из четырёх), ожиданий блокировок 0. Та же ступень на
cpx22 с тем же пулом давала 16 покупок из 750. Два лишних ядра укоротили
транзакцию удержания настолько, что очередь на строке сеанса не успевает
вырасти до размера пула.

Лестница с пулом 60 (1000 посетителей, ступени по 2 минуты, пул 6 600;
ступень 800 — отдельно на свежем пуле, потому что предыдущий кончился):

| заказов/мин | посетителей | ok | неудачных | ошибок | путь p95 | путь p99 | checkout p95 | выдача p95 | req/s | api / postgres CPU | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 200 | 1000 | 401 | 0 | 0 % | 82 мс | 2,3 с | 50 мс | 1,0 с | 258 | — | ok |
| 400 | 1000 | 801 | 0 | 0 % | 70 мс | 2,7 с | 44 мс | 1,0 с | 270 | 33–41 % / ~105 % | ok |
| 600 | 1000 | 1100 | 0 | 0 % | 63 мс | 2,2 с | 35 мс | 1,0 с | 282 | 37–41 % / ~105 % | ok (101 sold_out — кончился пул) |
| 800 | 1000 | 1600 | 0 | 0 % | 87 мс | 2,5 с | 54 мс | 1,0 с | 296 | 36–45 % / 115–145 % | ok |

На cpx32 виджет не сломался ни на одной ступени: **800 заказов/мин в один
сеанс при 1000 посетителей** — с запасом (api 40 %, postgres 1,2–1,4 ядра
из четырёх, ожиданий блокировок 0, k6 темп удержал, dropped 0). Дальше
поднимать нагрузку не позволяет генератор: 1000 VU — его потолок, а
именно толпа читателей, не покупатели, создаёт основной трафик
(~260–300 req/s на всех ступенях). p99 2–2,7 с на всех ступенях — это
хвост опроса статуса до появления билетов (выдача p95 1,0 с — такт
воркера), не деградация.

Ступени 600 и 800 на cpx22 не гонялись: на 2 vCPU 400 заказов/мин с пулом
60 уже давали postgres 63–80 % и p99 5,2 с.

### 4.6. cpx32 — шлюз, «как сайты пользуются шлюзом»

10 читателей каталога, ступени по 2 минуты, пул 6 600.

| заказов/мин | ok | неудачных | ошибок | 5xx | транспорт | путь p95 | путь p99 | CREATE_ORDER_EXT p95 | req/s | api / postgres CPU | вердикт |
|---|---|---|---|---|---|---|---|---|---|---|---|
| 200 | 401 | 0 | 0 % | 0 | 0 | 277 мс | 300 мс | 67 мс | 22 | — | ok |
| 400 | 790 | 11 | 0,26 % | 0 | 13 | 222 мс | 249 мс | 56 мс | 40 | 95–110 % / 75–100 % | 13 обрывов до стенда |
| 800 | 1526 | 0 | 0 % | 0 | 0 | 229 мс | 433 мс | 56 мс | 76 | 140–157 % / 100–130 % | ok |

На ступени 400 api завершил 5 087 запросов, все 200, максимум 295 мс,
Traefik без ошибок — 13 обрывов (0,25 %) случились до стенда (генератор
или сеть) и на ступени 800 не повторились. Команды на 800: RESERVATION
58 / 87 мс (медиана / p95), GET_ALL_ACTIONS 99 / 177, CREATE_ORDER_EXT
37 / 56, PAY_ORDER 24 / 43. Суммарно ~2,7 ядра из 4 — потолок шлюза на
cpx32 по CPU около 1 100–1 200 заказов/мин.

### 4.7. Через Cloudflare (оранжевое облако), cpx32, пул 60

`loadtest-api` переключён на прокси Cloudflare, в Traefik добавлены 22
диапазона Cloudflare в `forwardedHeaders.trustedIPs`, на api
`TRUSTED_PROXY_COUNT=3`. Проверка: запрос с `X-Forwarded-For: 10.9.8.7`
через Cloudflare api записал `remote_addr=10.9.8.7`.

| вход | заказов/мин | посетителей | ok | ошибок | 429 | путь p95 (серое облако) | путь p99 (серое) |
|---|---|---|---|---|---|---|---|
| виджет | 200 | 1000 | 401/401 | 0 | 0 | 185 мс (82) | 648 мс (2,3 с) |
| шлюз | 200 | 10 | 400/400 | 0 | — | 423 мс (277) | 478 мс (300) |

Cloudflare добавляет ~100–150 мс к пути покупки (nbg1 → edge → nbg1 на
каждый запрос) и ничего не режет; хвост p99 виджета даже короче — edge
сглаживает всплески соединений.

## 5. Матрица серверов

Что даёт каждый размер при **правильной настройке** (пул api 60,
`TRUSTED_PROXY_COUNT` по числу хопов, сеанс до ~6 000 мест):

| Сервер | Цена | Виджет (1000 посетителей) | Шлюз (WP-режим) | Во что упирается дальше |
|---|---|---|---|---|
| cpx22 — 2 vCPU, 4 ГБ | €0,031/ч (~€23/мес) | 400 заказов/мин чисто (p95 139 мс, p99 5 с) | 400 заказов/мин чисто (p95 228 мс) | CPU postgres (63–80 % на 400) и хвост p99 |
| **cpx32 — 4 vCPU, 8 ГБ (прод)** | €0,057/ч (~€42/мес) | ≥ 800 заказов/мин (p95 87 мс, p99 2,5 с), запас ×2 по CPU | 800 заказов/мин (p95 229 мс), потолок ~1 100 | генератор (1000 VU); CPU api на шлюзе |
| cpx42 — 8 vCPU, 16 ГБ | €0,111/ч (~€82/мес) | не мерилось (лимит ядер проекта); по CPU-профилю ≥ ×2 к cpx32 | — | — |

С **прод-настройкой как есть** (пул 20): cpx22 — 200 заказов/мин при 1000
посетителей (на 300–400 конвой); cpx32 — 300 при 1000 посетителей чисто
(898/898, p95 73 мс), выше не проверялось с пулом 20.

Уровни владельца против матрицы:

| Уровень | Посетителей / заказов в мин | Хватает |
|---|---|---|
| минимальный | 50 / 10 | cpx22 с любой настройкой |
| средний | 200 / 50 | cpx22 с любой настройкой |
| высокий | 1000 / 200 | cpx22 с пулом 60 (без запаса по p99); **cpx32 — с запасом ×4** |
| «ажиотаж» | 1000 / 400–800 в один сеанс | cpx32 с пулом 60 |

Ограничения матрицы: один сеанс на всю продажу (несколько событий
складываются, пока пул api не забит — §4.4); толпа не больше 1000
одновременных посетителей (генератор); пул мест 6 600 (для 20–50-тысячной
GA-категории — §3.1); всё с серым облаком, Cloudflare добавляет
100–150 мс.

## 6. Что исправить (по убыванию цены ошибки) — и что уже сделано

1. **`DB_POOL_MAX_CONNS` на проде поднять с 20 до 60** (Postgres
   `max_connections` 100, воркер держит ещё ~3). Самая дешёвая и самая
   действенная правка: она отделяет «медленно» от «встало». С 64
   соединениями postgres на копии занимал ~300 МБ — в `mem_limit` 1 ГБ
   помещается. **Статус: ждёт владельца** — сессия агента не имеет
   права деплоить на прод; ставится в Raw-compose `backend-prod` в
   Dokploy одним деплоем вместе с тегом образа `4434b93` и п. 3.
2. **Укоротить транзакцию удержания** в `hfeed` checkout/start.
   **Сделано 23.09 (ветка `loadtest-fixes`):** проверка платёжной
   конфигурации и промокод GA-корзины выполняются до открытия
   транзакции; цены мест, ценовые окна и лимиты промокода сидячей
   корзины читаются через соединение самой транзакции, а не через пул;
   то же в пути восстановления чекаута. Правило записано в AGENTS.md:
   внутри открытой транзакции — ни одного запроса через пул.
3. **Cloudflare перед прод-Traefik**: диапазоны Cloudflare в
   `entryPoints.web/websecure.forwardedHeaders.trustedIPs`
   (`/etc/dokploy/traefik/traefik.yml` на прод-хосте, затем
   `docker restart dokploy-traefik`) и `TRUSTED_PROXY_COUNT=2` (Cloudflare
   + Traefik) в Raw-compose прода. Сейчас на проде 1 — api видит адрес
   edge-узла Cloudflare, и per-IP лимит 600/мин делит на всех
   покупателей одного edge. Проверка та же, что в §4.7. **Статус: ждёт
   владельца** — скрипт `ops/server-bootstrap/traefik-trust-cloudflare.sh`
   (идемпотентный, с резервной копией конфига), запускается как
   `ssh arena-prod 'bash -s' < ops/server-bootstrap/traefik-trust-cloudflare.sh`,
   и только после него `TRUSTED_PROXY_COUNT: "2"` в compose.
4. **Индекс для GA-категорий.** **Сделано 23.09: миграция 0109**
   `session_seats_ga_tier_status_idx (session_id, tier_id, status,
   seat_key) WHERE kind='ga_unit'` — на локальном сеансе с 66 000 мест
   выделение трёх мест 150 мс → 0,2 мс, 2 130 буферов → 9.
5. **`capacity.mjs`**: **сделано 23.09** — при `STAND_EXEC` (или локально)
   лестница снимает `pg_stat_activity` на 60 % ступени и считает ответы
   api по его логу; вердикт «транспорт, не arena» теперь выдаётся только
   когда лог api за ступень чист.

### 6.1. Проверка правок 23.09

- Unit-тесты `hfeed`, `hcheckout`, `httpserver`, `migrations` — зелёные;
  интеграционные на свежей базе `arena_ci_lt` (миграции до 0109 + seed):
  все 17 сценариев hosted-чекаута (`TestHostedCheckout_*`), путь
  восстановления, промо, чекаут виджета — зелёные.
- Сквозной прогон через настоящий бинарник: образ собран из ветки,
  api + worker подняты отдельно на той же scratch-базе, k6-лестница
  виджета 100 и 200 заказов/мин по минуте (500 и 1000 посетителей): 101/101
  и 198/201, путь p95 51 и 49 мс, api-лог за ступени чист (8 896 и
  17 635 ответов, ни одного 5xx или дольше 5 с), в базе 0 ожиданий
  блокировок и 0 idle-in-transaction в середине ступени. Три обрыва на
  200/мин — NAT Docker Desktop на ноутбуке (локальный потолок, §4.1
  отчёта шага 1), и новая доказательная база `capacity.mjs` так и
  написала.

## 7. Уборка

После сохранения данных (`docs/loadtest/data/2026-09-22_server/`): удалены
оба сервера и файрвол `arena-loadtest-lab` через API Hetzner, три
DNS-записи `loadtest-*` через API Cloudflare, алиасы `arena-loadtest` /
`arena-loadgen` из `~/.ssh/config`. Токены (`api-token-*.txt` в корне
репозитория, вне git) владелец отзывает сам.


## 8. Не переехать ли на сервер подешевле

Факты (23.09, API Hetzner, цены с НДС в месяц): прод `cpx32` — 4 vCPU,
8 ГБ, диск 160 ГБ, €35,49; `cpx22` — 2 vCPU, 4 ГБ, 80 ГБ, €19,49; `cx33`
(Intel, 4 vCPU, 8 ГБ, 80 ГБ) — €8,49; `cax21` (ARM, 4 vCPU, 8 ГБ, 80 ГБ) —
€10,49. Нагрузка на проде сейчас близка к нулю: load 0,16, память 950 МБ
из 7,7 ГБ, диск 9 ГБ из 150.

- **Rescale вниз невозможен.** Hetzner не уменьшает диск: у cpx32 он
  160 ГБ, у всех вариантов дешевле — 80 ГБ и меньше. «Переехать» значит
  создать новый сервер, перенести базу и стек через Dokploy, переключить
  DNS — час-два работы и окно недоступности.
- **Экономия от cpx22 — €16 в месяц**, при этом высокую нагрузку он
  держит только с пулом 60 и без запаса по хвосту задержек (§4.4). За
  риск переезда и потерю четырёхкратного запаса это слишком мало.
- **Если переезжать когда-нибудь всё равно** (например, при уходе с
  Dokploy-панели на lead-parser или при переносе в другой регион), то
  реальные варианты — `cx33` за €8,49 (та же формула 4 vCPU / 8 ГБ, но
  Intel-линейку 22.09 не размещали ни в одном немецком ДЦ) или `cax21`
  за €10,49 (ARM: нужен образ `arena-api` под arm64, сейчас собирается
  только amd64). Оба в 3–4 раза дешевле прода при той же формуле.
- **Где деньги на самом деле:** аудит инфраструктуры 17.09
  (`docs/ops/infra_audit_2026-09-17_ru.md`) — четыре сервера, из них два
  legacy под старую интеграцию; их отключение после cutover с Bil24 даст
  больше, чем любой rescale прода.

Рекомендация: прод оставить как есть до cutover, вернуться к вопросу
вместе с отключением legacy-серверов.
